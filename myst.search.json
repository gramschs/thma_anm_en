{"version":"1","records":[{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks"},"type":"lvl1","url":"/chapter01-sec00","position":0},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks"},"content":"In the following chapters we write and test Python code in Jupyter\nNotebooks. This page shows in a few minutes how to operate a notebook.\nIf you have already worked with notebooks, you can jump straight to\nChapter 1.1.","type":"content","url":"/chapter01-sec00","position":1},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"What is a Jupyter Notebook?"},"type":"lvl2","url":"/chapter01-sec00#what-is-a-jupyter-notebook","position":2},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"What is a Jupyter Notebook?"},"content":"A Jupyter notebook is an interactive document that combines text, Python code\nand outputs in a single file. We recognize notebook files by the .ipynb\nextension. A notebook consists of individual cells. There are two kinds:\n\nCode cells contain Python code. When we run a code cell, Python\nevaluates the code and shows the result directly below it.\n\nText cells contain explanatory text. They are written in the\nMarkdown language and are displayed as formatted text when run.\n\nThese lecture notes are themselves built from notebooks: the running text\nsits in text cells, and the gray-shaded code blocks are executable code\ncells.","type":"content","url":"/chapter01-sec00#what-is-a-jupyter-notebook","position":3},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"Running a cell"},"type":"lvl2","url":"/chapter01-sec00#running-a-cell","position":4},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"Running a cell"},"content":"We click the cell and then press Shift+Enter. Alternatively, there is a\nbutton with a triangle (Run) in the toolbar. The result appears immediately\nbelow the cell.\n\n3 * (7 - 10) + 5\n\n\n\nIf the last line of a code cell evaluates to a value, the notebook displays\nthat value automatically. If we want to see several values or intermediate\nresults, we use the print() function.\n\nprint(3 / 4)\nprint(2 ** 10)\n\n\n\nTo the left of an executed code cell there is a number in square brackets,\nfor example [1]. This is the execution counter. It shows in which\nposition this cell was run.\n\nShift+Enter runs the cell and moves to the next one. If we want to stay in\nthe same cell after running it, we press Ctrl+Enter.\n\nWith the + button in the toolbar we insert a new cell. We delete a selected\ncell with the trash-can icon or, when the cell has a blue border, by pressing\nthe d key twice.","type":"content","url":"/chapter01-sec00#running-a-cell","position":5},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"Order matters"},"type":"lvl2","url":"/chapter01-sec00#order-matters","position":6},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"Order matters"},"content":"A notebook does not run cells from top to bottom by itself. It runs them\nin the order in which we click and start them. The execution counter [1],\n[2], [3] shows this order.\n\nThis matters as soon as one cell builds on another. In the following example,\nthe first cell creates the variable mass, and the second one computes with\nit.\n\nmass = 1200\n\n\n\nprint(mass * 2)\n\n\n\nIf we run the second cell without having run the first one beforehand, Python\ndoes not yet know the variable mass and reports a NameError.\n\nIf the state of the notebook has become muddled, a restart helps: from the\nKernel menu we choose Restart Kernel and Run All Cells. This makes Python\nforget all variables and run the whole notebook afresh from top to bottom.","type":"content","url":"/chapter01-sec00#order-matters","position":7},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"When an error message appears"},"type":"lvl2","url":"/chapter01-sec00#when-an-error-message-appears","position":8},{"hierarchy":{"lvl1":"Excursus: Working with Jupyter Notebooks","lvl2":"When an error message appears"},"content":"Error messages are normal and part of programming. The last line of the\nmessage names the error type and a short description, for example:NameError: name 'mass' is not defined\n\nThis message tells us: Python does not know the variable mass. Usually this\nis due to a typo in the name or to the cell with the assignment not having\nbeen run yet.\n\nThat covers everything we need to get started. We continue with Chapter 1.1.","type":"content","url":"/chapter01-sec00#when-an-error-message-appears","position":9},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures"},"type":"lvl1","url":"/chapter01-sec01","position":0},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures"},"content":"In vehicle development, sensors continuously deliver measured values: the\nspeed of a test vehicle, the force on a test rig or the temperature of a\ncomponent. Before we analyze such measurement series with numerical methods\nin the following chapters, we need the tools of the Python programming\nlanguage. In this chapter we learn how to store individual measured values\nin variables, compute with them and control the flow of our program with\nbranches and loops.\n\nWe work with Jupyter notebooks. A notebook consists of individual code\ncells that we run one after another. The result of a cell appears directly\nbelow it, so that we can build up and test our code step by step. Let us try\nthis right away with a first example.\n\nIf you have never programmed before, you can watch the short videos as\npreparation; they are embedded at the end of each section in this chapter.","type":"content","url":"/chapter01-sec01","position":1},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"Learning objectives"},"type":"lvl2","url":"/chapter01-sec01#learning-objectives","position":2},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"Learning objectives"},"content":"Learning objectives\n\nYou know the data types integer, float and string and can\ntell them apart with type().\n\nYou can create variables, print them with the print() function and\nformat them with f-strings.\n\nYou know arithmetic, comparison and logical operators and the\ndifference between assignment = and comparison ==.\n\nYou can branch a program flow with if/elif/else.\n\nYou can write a for loop with range().","type":"content","url":"/chapter01-sec01#learning-objectives","position":3},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How do we store measured values in variables?"},"type":"lvl2","url":"/chapter01-sec01#how-do-we-store-measured-values-in-variables","position":4},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How do we store measured values in variables?"},"content":"Imagine a sensor on a test vehicle measures the current speed. We want to\nstore this value along with further information about the measurement in our\nprogram.\n\nspeed = 27.8\nnum_measurements = 50\nsensor_name = 'Sensor_A'\n\n\n\nWe call speed, num_measurements and sensor_name variables. A\nvariable is like a labeled drawer: we give it a name and put a value inside.\nPutting a value inside is called assignment, and we use the equals sign\n= for it. Technically, the drawer is a small area in the computer’s memory.\n\nNext we print the content of the variables to the monitor or display. For\nthis we use Python’s built-in print() function.\n\nprint(speed)\nprint(num_measurements)\nprint(sensor_name)\n\n\n\nWe recognize functions by the parentheses after the function name. Into\nthese parentheses goes the so-called argument. An argument is, so to\nspeak, the input passed to the function so that Python knows which value to\ndisplay on the screen.\n\nPython distinguishes the values in a variable by their data type. In\nthis section we introduce three simple data types:\n\nInteger: a whole number, for example the number of measurements.\n\nFloat: a decimal number, for example a speed in km/h. The decimal\nseparator in Python is always a point, never a comma.\n\nString: a sequence of characters, for example the name of a sensor.\nStrings are enclosed in single or double quotation marks.\n\nWith the type() function we display the data type of a variable.\n\nprint(type(speed))\nprint(type(num_measurements))\nprint(type(sensor_name))\n\n\n\nVariables can also be changed later. We often write code like the following\nexample to increase a variable by a value.\n\nnum_measurements = num_measurements + 1\nprint(num_measurements)\n\n\n\nDoesn’t that contradict mathematics? In mathematics we would be allowed to\nsubtract num_measurements from both sides of the equation and would arrive\nat 0 = 1, which is obviously false. In Python, however, the\nequals sign = is not a mathematical equation but an assignment. Python\nfirst computes the value on the right-hand side and then stores the result\nin the variable on the left-hand side.\n\nTo print values together with text, we use f-strings. For this we put an\nf directly in front of the quotation mark and write the variable name in\ncurly braces.\n\nprint(f'The sensor {sensor_name} measures a speed of {speed} km/h.')\n\n\n\nFor floats we can additionally specify the number of decimal places. For\nthis we write a colon after the variable name, followed by the desired\nnumber of decimal places and an f.\n\npi = 3.141592653589793\nprint(f'Pi to two decimal places: {pi:.2f}')\n\n\n\nMini-exercise (✩)\n\nCreate three variables for a material sample: mass (float, in kg),\nnum_tests (integer) and material (string, for example 'steel'). Print\nall three values with print() and then display the data type of mass\nwith type().\n\nAlso answer, without running the code: which data type would type() return\nif you wrote mass = 5 without a decimal point instead? Justify your answer.\n\n# code cell\n\n\n\nSolutionmass = 4.85\nnum_tests = 3\nmaterial = 'steel'\n\nprint(mass)\nprint(num_tests)\nprint(material)\nprint(type(mass))\n\nWith mass = 5 without a decimal point, type() would return the data type\nint, because Python interprets a number without a point as a whole number.\nOnly the decimal point turns a number into a float.\n\nVideo “Zahlen in Python” by Programmieren Starten\n\nVideo “Strings” by Programmieren Starten\n\nVideo “Variablen” by Programmieren Starten\n\nVideo “Datentypen” by Programmieren Starten","type":"content","url":"/chapter01-sec01#how-do-we-store-measured-values-in-variables","position":5},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How do we compute and compare in Python?"},"type":"lvl2","url":"/chapter01-sec01#how-do-we-compute-and-compare-in-python","position":6},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How do we compute and compare in Python?"},"content":"From the introductory physics lecture we already know the SI unit m/s for\nspeed. Sensor data, however, is often given in km/h, so we first convert.\n\nspeed_kmh = 100\nspeed_ms = speed_kmh / 3.6\nprint(speed_ms)\n\n\n\nPython knows the usual arithmetic operators: + for addition, - for\nsubtraction, * for multiplication, / for division and ** for\nexponentiation. With these operators we compute, for example, the kinetic\nenergy of a vehicle.\n\nmass = 1200        # kg\nspeed_ms = 27.8    # m/s\n\nkinetic_energy = 0.5 * mass * speed_ms**2\nprint(f'Kinetic energy: {kinetic_energy:.1f} joules')\n\n\n\nBesides computing with numbers, we often need to compare values as well, for\nexample to check whether a speed limit is being observed. For this we have\ncomparison operators: >, <, >=, <=, == for “is equal to” and\n!= for “is not equal to”. The result of a comparison is always a truth\nvalue, that is, either True or False.\n\nspeed_limit_ms = 33.3   # corresponds to 120 km/h\n\nprint(speed_ms > speed_limit_ms)\nprint(speed_ms == speed_limit_ms)\n\n\n\nCaution: = is not the same as ==\n\nThe single equals sign = is the assignment operator and stores a value in\na variable. The double equals sign == compares two values and returns\nTrue or False. This mix-up is one of the most common beginner mistakes\nin Python.\n\nWe combine several conditions with the logical operators and and or.\nWith and both conditions must be true, with or one is enough.\n\nis_fast = speed_ms > 25\nis_within_limit = speed_ms <= speed_limit_ms\n\nprint(is_fast and is_within_limit)\n\n\n\nWith the not operator we invert a truth value: True becomes False and\nvice versa.\n\nMini-exercise (✩)\n\nCompute the braking distance of a vehicle using the rule of thumb\nbraking_distance = (speed_kmh / 10) ** 2 / 2. Use speed_kmh = 80 and\nprint the result with an f-string and one decimal place.\n\nAlso answer without running the code: what does the expression 5 == 5.0\nreturn, even though an integer and a float are being compared? Justify your\nanswer.\n\n# code cell\n\n\n\nSolutionspeed_kmh = 80\nbraking_distance = (speed_kmh / 10) ** 2 / 2\nprint(f'Braking distance: {braking_distance:.1f} m')\n\n5 == 5.0 returns True. With the == operator, Python compares the\nnumeric value, not the data type. Since 5 and 5.0 have the same numeric\nvalue, the comparison is true, even though type(5) and type(5.0) yield\ndifferent data types.\n\nVideo “Zuweisungsoperator” by Programmieren Starten\n\nVideo “Vergleiche” by Programmieren Starten\n\nVideo “Logische Operatoren” by Programmieren Starten","type":"content","url":"/chapter01-sec01#how-do-we-compute-and-compare-in-python","position":7},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How does our program control the flow?"},"type":"lvl2","url":"/chapter01-sec01#how-does-our-program-control-the-flow","position":8},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"How does our program control the flow?"},"content":"So far, every line of our program was executed exactly once. Often, however,\nwe want to run different code depending on a condition, for example depending\non whether a speed limit is being observed.\n\nspeed_ms = 35.0\nspeed_limit_ms = 33.3\n\nif speed_ms > speed_limit_ms:\n    print('Speed too high!')\nelif speed_ms == speed_limit_ms:\n    print('Speed exactly at the limit.')\nelse:\n    print('Speed within the permitted range.')\n\n\n\nWe call this structure a branch. The line with if is called the\nheader and is terminated with a colon :. All statements that belong to\nthis condition are indented by the same number of spaces. This indentation\nshows Python which code belongs together. With elif (short for “else if”)\nwe check a further condition, with else we catch all remaining cases.\n\nOften we need to repeat a statement several times, for example to simulate\nseveral time steps of a measurement. For this we use the for loop\ntogether with the range() function.\n\nfor time_step in range(5):\n    print(f'Time step {time_step}: measurement is being carried out.')\n\n\n\nThe header consists of the fixed keywords for and in. Between them is\ntime_step, the loop variable. On each pass it takes the next value from\nrange(5), that is, 0, 1, 2, 3 and 4 in turn, because range(5) generates\nfive numbers starting at 0. The indented lines below form the loop body\nand are executed on each pass.\n\nWithin the loop body we may also use a branch, for example to check for each\ntime step whether a limit value is exceeded.\n\nfor time_step in range(5):\n    speed_ms = time_step * 12.0\n    if speed_ms > speed_limit_ms:\n        print(f'Time step {time_step}: {speed_ms:.1f} m/s, too fast!')\n    else:\n        print(f'Time step {time_step}: {speed_ms:.1f} m/s, within the limit.')\n\n\n\nThe loop runs through all five time steps. On each pass we compute the\ncurrent speed and decide with the if check which message is printed.\n\nMini-exercise (✩)\n\nWrite a for loop with range(5) that, starting from 0 km/h, increases the\nspeed by 30 km/h on each pass of the loop and prints the respective value.\n\nAdd an if check in the loop body that additionally prints too fast as\nsoon as the speed exceeds 80 km/h. Answer before running: in which passes\ndoes too fast appear? Justify your answer.\n\n# code cell\n\n\n\nSolutionfor time_step in range(5):\n    speed_kmh = time_step * 30\n    print(f'Time step {time_step}: {speed_kmh} km/h')\n    if speed_kmh > 80:\n        print('too fast')\n\nThe loop variable time_step runs through the values 0 to 4, the speed takes\nthe values 0, 30, 60, 90 and 120 km/h. too fast appears in passes 3 and 4,\nbecause there the speed is 90 and 120 km/h respectively, above the limit of\n80 km/h.\n\nVideo “if-Anweisung” by Programmieren Starten\n\nVideo “if, elif, else” by Programmieren Starten\n\nVideo “for-Schleife in Python: Zählerschleife” by Programmieren Starten","type":"content","url":"/chapter01-sec01#how-does-our-program-control-the-flow","position":9},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"Summary and outlook"},"type":"lvl2","url":"/chapter01-sec01#summary-and-outlook","position":10},{"hierarchy":{"lvl1":"1.1 Variables, Operators and Control Structures","lvl2":"Summary and outlook"},"content":"In this chapter we covered the basic building blocks of Python: variables\nfor storing measured values, the data types integer, float and string,\narithmetic, comparison and logical operators, as well as branching with\nif/elif/else and the for loop for repetition. With these we can\nalready process individual values and automate recurring calculations.\n\nWhat we still lack, however, is a way to manage several measured values\ntogether. In the next chapter, 1.2, we deepen what we have learned with\ntest-rig tasks. In chapter 1.3 we then introduce the data structures\nlist and dictionary and write our own functions to encapsulate\nrecurring calculations such as the conversion from km/h to m/s. In chapter 2\nwe finally use NumPy and Matplotlib to process and plot entire\nmeasurement series without loops.","type":"content","url":"/chapter01-sec01#summary-and-outlook","position":11},{"hierarchy":{"lvl1":"1.2 In-depth Practice (Part 1)"},"type":"lvl1","url":"/chapter01-sec02","position":0},{"hierarchy":{"lvl1":"1.2 In-depth Practice (Part 1)"},"content":"The following exercises build on the code-along from chapter 1.1. Exercises\nwith one star (✩) and two stars (✩✩) are compulsory. The exercise with three\nstars (✩✩✩) is a bonus exercise for anyone who finishes early. Work in pairs\nif possible; this helps especially if Python is still new to you. If you want\nto look up a term, the cheat sheet at the end of Part 1 will help.\n\nWarm-up exercise (✩)\n\nA test vehicle travels at 27.8 m/s. Create a variable speed_ms with this\nvalue and a variable vehicle with the value 'test_car_3'. Print both with\nprint() and display the data type of speed_ms with type().\n\n# code cell\n\n\n\nSolutionspeed_ms = 27.8\nvehicle = 'test_car_3'\n\nprint(speed_ms)\nprint(vehicle)\nprint(type(speed_ms))\n\nspeed_ms is a float, because 27.8 contains a decimal point.\n\nExercise A (✩)\n\nA test vehicle delivers various measured values. Which data type is present\nin each case? Write down your guess first, before you run the code.\n\n120 (number of measurements) -->\n\n27.8 (speed in m/s) -->\n\n'test_car_3' (vehicle name) -->\n\n120 / 4 -->\n\n2 ** 8 -->\n\n27.8 > 33.3 -->\n\nThen check each line with type() in a code cell.\n\n# code cell\n\n\n\nSolutionprint(type(120))\nprint(type(27.8))\nprint(type('test_car_3'))\nprint(type(120 / 4))\nprint(type(2 ** 8))\nprint(type(27.8 > 33.3))\n\n120 is an integer, 27.8 a float and 'test_car_3' a string. 120 / 4\nalways yields a float with the / operator, even if the result is a whole\nnumber. 2 ** 8 stays an integer, because both operands are integers. The\ncomparison 27.8 > 33.3 yields the data type bool, because comparisons\nalways return a truth value.\n\nExercise B (✩)\n\nThe following code is given. Note down what it prints before you run it.speed_ms = 30.0\nspeed_limit_ms = 33.3\n\nif speed_ms > speed_limit_ms:\n    print('too fast')\nelif speed_ms == speed_limit_ms:\n    print('exactly at the limit')\nelse:\n    print('within the limit')\n\nWhat changes about the output if the first line reads speed_ms = 33.3?\n\n# code cell\n\n\n\nSolutionspeed_ms = 30.0\nspeed_limit_ms = 33.3\n\nif speed_ms > speed_limit_ms:\n    print('too fast')\nelif speed_ms == speed_limit_ms:\n    print('exactly at the limit')\nelse:\n    print('within the limit')\n\nWith speed_ms = 30.0 the first condition is false and the second one too,\nso the else branch takes effect and the output is within the limit. With\nspeed_ms = 33.3 the first condition is still false, but the second one\n(==) is true, so the output is then exactly at the limit.\n\nExercise C (✩✩)\n\nComplete the code at the places marked with ___. A test vehicle travels at\nspeed_kmh = 95. The code should convert the speed to m/s and print whether\nthe speed limit of 33.3 m/s is exceeded.speed_kmh = 95\nspeed_limit_ms = 33.3\n\nspeed_ms = speed_kmh / ___\n\nif speed_ms ___ speed_limit_ms:\n    print(f'{speed_ms:.1f} m/s: speed limit exceeded')\nelse:\n    print(f'{speed_ms:.1f} m/s: within the permitted range')\n\n# code cell\n\n\n\nSolutionspeed_kmh = 95\nspeed_limit_ms = 33.3\n\nspeed_ms = speed_kmh / 3.6\n\nif speed_ms > speed_limit_ms:\n    print(f'{speed_ms:.1f} m/s: speed limit exceeded')\nelse:\n    print(f'{speed_ms:.1f} m/s: within the permitted range')\n\nThe first gap is 3.6, because 1 m/s corresponds to 3.6 km/h. The second gap\nis the comparison operator >. 95 km/h corresponds to about 26.4 m/s, which\nis below the speed limit, so the output is\n26.4 m/s: within the permitted range.\n\nExercise D (✩✩)\n\nComplete the for loop at the ___ places. An acceleration test runs over\nseven time steps. In each time step the speed increases by 7 m/s. The code\nshould print the speed for each time step and additionally too fast as\nsoon as it is above the speed limit of 33.3 m/s.speed_limit_ms = 33.3\n\nfor time_step in range(7):\n    speed_ms = time_step * ___\n    print(f'Time step {time_step}: {speed_ms:.1f} m/s')\n    if ___:\n        print('too fast')\n\n# code cell\n\n\n\nSolutionspeed_limit_ms = 33.3\n\nfor time_step in range(7):\n    speed_ms = time_step * 7.0\n    print(f'Time step {time_step}: {speed_ms:.1f} m/s')\n    if speed_ms > speed_limit_ms:\n        print('too fast')\n\nThe first gap is 7.0 (increase per time step), the second one the condition\nspeed_ms > speed_limit_ms. The speed takes the values 0, 7, 14, 21, 28, 35\nand 42 m/s. too fast appears in time steps 5 and 6.\n\nExercise E (✩✩✩, mini-project)\n\nA test rig simulates an acceleration test over ten time steps. Implement the\nfollowing steps.\n\nPart 1: Write a for loop with range(10) for the time steps 0 to 9. In\neach time step the speed is speed_ms = time_step * 5.0. Print the time step\nand speed with an f-string.\n\nPart 2: Add an if/else check in the loop body that prints for each\ntime step whether the speed limit speed_limit_ms = 33.3 is observed or\nexceeded.\n\nPart 3: Count in how many time steps the speed limit is exceeded. For\nthis, create a variable count_too_fast = 0 before the loop and increase it\nby 1 in the appropriate case. Print the number after the loop.\n\nClosing question: From which time step onward is the speed limit\nexceeded, and how does that relate to the chosen acceleration of 5.0 m/s per\ntime step?\n\n# code cell\n\n\n\nSolutionspeed_limit_ms = 33.3\ncount_too_fast = 0\n\nfor time_step in range(10):\n    speed_ms = time_step * 5.0\n    if speed_ms > speed_limit_ms:\n        print(f'Time step {time_step}: {speed_ms:.1f} m/s, too fast')\n        count_too_fast = count_too_fast + 1\n    else:\n        print(f'Time step {time_step}: {speed_ms:.1f} m/s, within the limit')\n\nprint(f'Too fast in {count_too_fast} time steps.')\n\nThe speed takes the values 0, 5, 10, ... up to 45 m/s. The speed limit is\nexceeded from time_step = 7 onward, because there the speed is\n7 * 5.0 = 35.0 m/s and is above 33.3 m/s for the first time. In total the\nvehicle is too fast in time steps 7, 8 and 9, that is, in three time steps.\nThe variable count_too_fast acts as a counter that we increase by 1 in each\nmatching pass. With a larger acceleration per time step, the limit would be\nexceeded earlier.","type":"content","url":"/chapter01-sec02","position":1},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions"},"type":"lvl1","url":"/chapter01-sec03","position":0},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions"},"content":"In chapter 1.1 we stored individual measured values in variables and used a\nbranch to check whether a speed limit is being observed. During an\nacceleration test, however, a test vehicle delivers not just a single\nmeasured value but a whole measurement series. A single variable is no longer\nenough for that. In this chapter we therefore introduce the data structures\nlist and dictionary, with which we manage several values together.\nAfterwards we encapsulate recurring calculations such as the conversion from\nkm/h to m/s in our own functions.\n\nAs in chapter 1.1, short videos are again embedded at the end of the sections\nas preparation.","type":"content","url":"/chapter01-sec03","position":1},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"Learning objectives"},"type":"lvl2","url":"/chapter01-sec03#learning-objectives","position":2},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"Learning objectives"},"content":"Learning objectives\n\nYou can create lists, extend them with append() and access\nelements via the index.\n\nYou can create, read and modify dictionaries with key-value pairs.\n\nYou can write your own functions with def, use parameters and\nreturn a value with return.\n\nYou know what a default value and a docstring are.","type":"content","url":"/chapter01-sec03#learning-objectives","position":3},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we collect several measured values in a list?"},"type":"lvl2","url":"/chapter01-sec03#how-do-we-collect-several-measured-values-in-a-list","position":4},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we collect several measured values in a list?"},"content":"Imagine an acceleration test in which a sensor records the speed at several\npoints in time. We collect these values in a list, recognizable by the\nsquare brackets.\n\nspeeds_kmh = [80, 95, 120, 60, 110]\nprint(speeds_kmh)\n\n\n\nA list can contain any number of elements. With len() we display the\nnumber of elements.\n\nnum_measurements = len(speeds_kmh)\nprint(f'Number of measurements: {num_measurements}')\n\n\n\nWe access individual elements via the index. Python starts counting at 0.\nWith the index -1 we conveniently access the last element.\n\nfirst_measurement = speeds_kmh[0]\nlast_measurement = speeds_kmh[-1]\nprint(f'First measurement: {first_measurement} km/h')\nprint(f'Last measurement: {last_measurement} km/h')\n\n\n\nTo add a new measurement at the end of the list, we use the append()\nmethod.\n\nspeeds_kmh.append(75)\nprint(speeds_kmh)\n\n\n\nIf we want to process every measured value in a list, we iterate over the\nlist directly with a for loop, without the detour via range() and the\nindex.\n\nfor speed in speeds_kmh:\n    print(f'Measured value: {speed} km/h')\n\n\n\nMini-exercise (✩)\n\nCreate a list temperatures with five temperature readings of your choice.\nAppend another reading with append() and then print the number of elements\nwith len().\n\nAlso answer, without running the code: what does temperatures[-2] return\nafter you have appended the sixth value? Justify your answer.\n\n# code cell\n\n\n\nSolutiontemperatures = [18.5, 19.2, 21.0, 22.4, 20.1]\ntemperatures.append(23.7)\ncount = len(temperatures)\nprint(f'Number of readings: {count}')\n\ntemperatures[-2] returns the second-to-last element of the list, that is,\nthe value 20.1. The index -1 points to the last element (23.7, the\nvalue just appended), -2 points to the element directly before it.\n\nVideo “Listen in Python - Einführung” by Programmieren Starten\n\nVideo “Zugriff auf Listen” by Programmieren Starten\n\nVideo “for-Schleife mit Listen” by Programmieren Starten","type":"content","url":"/chapter01-sec03#how-do-we-collect-several-measured-values-in-a-list","position":5},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we structure data with keys?"},"type":"lvl2","url":"/chapter01-sec03#how-do-we-structure-data-with-keys","position":6},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we structure data with keys?"},"content":"A list like [200.0, 'test_rig_1', 'Sensor_A'] stores several values, but\nhow do we keep track, in a longer list, of which value stands for what? At\nindex 0 we would have to remember that the measuring range is there, at index\n1 the location and at index 2 the name. For such cases the dictionary is\nmore suitable, since we access it via meaningful keys instead of a numeric\nindex.\n\nsensor = {\n    'name': 'Sensor_A',\n    'location': 'test_rig_1',\n    'measuring_range_max': 200.0\n}\nprint(sensor)\n\n\n\nA value is accessed via the corresponding key in square brackets.\n\nprint(f'Sensor: {sensor[\"name\"]}')\nprint(f'Location: {sensor[\"location\"]}')\n\n\n\nWe can change existing values or add new key-value pairs.\n\nsensor['measuring_range_max'] = 250.0\nsensor['calibration_date'] = '2026-01-15'\nprint(sensor)\n\n\n\nMini-exercise (✩)\n\nCreate a dictionary measurement for a temperature reading with the keys\ntemperature (23.5), location ('test_rig_2') and timestamp\n('14:32'). Print the temperature and location with suitable labels.\n\nAlso answer: why would a list [23.5, 'test_rig_2', '14:32'] be less\nsuitable for this data than a dictionary? Phrase your answer in your own\nwords.\n\n# code cell\n\n\n\nSolutionmeasurement = {\n    'temperature': 23.5,\n    'location': 'test_rig_2',\n    'timestamp': '14:32'\n}\nprint(f'Temperature: {measurement[\"temperature\"]} degrees Celsius')\nprint(f'Location: {measurement[\"location\"]}')\n\nWith the list we would have to remember that index 0 contains the\ntemperature, index 1 the location and index 2 the timestamp. This mapping is\nnot obvious to others and is error-prone as soon as the order changes. The\ndictionary makes the meaning of each value immediately visible via the key.\n\nVideo “Dictionaries” by Pitrium","type":"content","url":"/chapter01-sec03#how-do-we-structure-data-with-keys","position":7},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we encapsulate calculations in functions?"},"type":"lvl2","url":"/chapter01-sec03#how-do-we-encapsulate-calculations-in-functions","position":8},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"How do we encapsulate calculations in functions?"},"content":"In chapter 1.1 we wrote out the formula speed_kmh / 3.6 for the conversion\nto m/s by hand several times. With our own function we encapsulate this\ncalculation, so that we only define it once and can reuse it as often as we\nlike.\n\ndef kmh_to_ms(speed_kmh):\n    speed_ms = speed_kmh / 3.6\n    return speed_ms\n\nspeed_ms = kmh_to_ms(95)\nprint(speed_ms)\n\n\n\nA function begins with the keyword def, followed by the function name and\nthe parameters in parentheses. Here too, the header ends with a colon :\nand the function body is indented. The keyword return determines which\nvalue is returned to the caller. Once the function is defined, we\ncall it as often as we like with different arguments, for example for every\nmeasured value in our list.\n\nfor speed in speeds_kmh:\n    print(f'{speed} km/h corresponds to {kmh_to_ms(speed):.1f} m/s')\n\n\n\nA function can have several parameters. If we write an = with a value after\na parameter, that is a default value: if we call the function without\nthis argument, Python automatically uses the default value.\n\ndef kinetic_energy(speed_ms, mass=1200):\n    return 0.5 * mass * speed_ms**2\n\nprint(f'{kinetic_energy(27.8):.1f} joules')            # mass = 1200\nprint(f'{kinetic_energy(27.8, mass=1500):.1f} joules')\n\n\n\nDirectly below the header of a function we can write a docstring in\ntriple quotation marks, a short sentence that describes what the function\ndoes.\n\ndef kmh_to_ms(speed_kmh):\n    \"\"\"Converts a speed from km/h to m/s.\"\"\"\n    return speed_kmh / 3.6\n\n\n\nVideo “Funktionen selbst definieren” by Programmieren Starten\n\nVideo “Funktionen mit Parametern” by Programmieren Starten\n\nVideo “Funktionen mit Rückgabewert” by Programmieren Starten\n\nMini-exercise (✩)\n\nWrite a function braking_distance(speed_kmh) that returns the braking\ndistance according to the rule of thumb (speed_kmh / 10) ** 2 / 2. Call the\nfunction for speed_kmh = 100 and print the result.\n\nAlso answer, without running the code: what does the function call return if\nyou forget the keyword return in the function? Justify your answer.\n\n# code cell\n\n\n\nSolutiondef braking_distance(speed_kmh):\n    return (speed_kmh / 10) ** 2 / 2\n\nprint(f'Braking distance: {braking_distance(100):.1f} m')\n\nWithout return, the function automatically returns the value None. The\ncalculation inside the function would still be carried out, but the result\nwould be lost, since it is not returned to the caller.","type":"content","url":"/chapter01-sec03#how-do-we-encapsulate-calculations-in-functions","position":9},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"Summary and outlook"},"type":"lvl2","url":"/chapter01-sec03#summary-and-outlook","position":10},{"hierarchy":{"lvl1":"1.3 Lists, Dictionaries and Functions","lvl2":"Summary and outlook"},"content":"In this chapter we have learned how to collect several measured values in a\nlist, structure them with meaningful keys in a dictionary and\nencapsulate recurring calculations in our own functions with parameters\nand return values. With these we now have the tools to process the\nmeasurement series from our acceleration tests.\n\nWe have deliberately left out some Python topics and will catch up on them\nlater, when we need them, for example error handling with try and except\nin the numerical methods. In chapter 2 we use NumPy and Matplotlib to\nprocess entire measurement series such as speeds_kmh all at once without our\nown loop and to plot them.","type":"content","url":"/chapter01-sec03#summary-and-outlook","position":11},{"hierarchy":{"lvl1":"1.4 In-depth Practice (Part 2)"},"type":"lvl1","url":"/chapter01-sec04","position":0},{"hierarchy":{"lvl1":"1.4 In-depth Practice (Part 2)"},"content":"The following exercises build on the code-along from chapter 1.3. Exercises\nwith one star (✩) and two stars (✩✩) are compulsory. The exercise with three\nstars (✩✩✩) is a bonus exercise for anyone who finishes early. Work in pairs\nif possible. If you want to look up a term, the cheat sheet at the end of\nPart 1 will help.\n\nWarm-up exercise (✩)\n\nAn acceleration test delivers the speeds 72, 88 and 95 km/h. Create\nthem as a list speeds_kmh. Print the first and the last element, and the\nnumber of elements with len().\n\n# code cell\n\n\n\nSolutionspeeds_kmh = [72, 88, 95]\n\nprint(speeds_kmh[0])\nprint(speeds_kmh[-1])\nprint(len(speeds_kmh))\n\nspeeds_kmh[0] is 72, the first element, speeds_kmh[-1] is 95, the\nlast. len(...) is 3, the number of elements.\n\nExercise A (✩)\n\nThe list speeds_kmh = [72, 88, 95, 60, 110, 130] is given. Note down your\nguess first, before you run the code.\n\nspeeds_kmh[0] -->\n\nspeeds_kmh[2] -->\n\nspeeds_kmh[-1] -->\n\nlen(speeds_kmh) -->\n\nThen check each line in a code cell.\n\n# code cell\n\n\n\nSolutionspeeds_kmh = [72, 88, 95, 60, 110, 130]\nprint(speeds_kmh[0])\nprint(speeds_kmh[2])\nprint(speeds_kmh[-1])\nprint(len(speeds_kmh))\n\nspeeds_kmh[0] is 72, the first element. speeds_kmh[2] is 95, the\nelement at index 2. speeds_kmh[-1] is 130, the last element. len(...)\nis 6, the number of elements.\n\nExercise B (✩)\n\nThe following code is given. Note down which lines it prints and which value\ncount has at the end, before you run it.speeds_kmh = [72, 88, 95, 60, 110, 130]\ncount = 0\n\nfor speed in speeds_kmh:\n    if speed > 90:\n        print(f'{speed} km/h: fast')\n        count = count + 1\n\nprint(f'Number of fast measurements: {count}')\n\n# code cell\n\n\n\nSolutionspeeds_kmh = [72, 88, 95, 60, 110, 130]\ncount = 0\n\nfor speed in speeds_kmh:\n    if speed > 90:\n        print(f'{speed} km/h: fast')\n        count = count + 1\n\nprint(f'Number of fast measurements: {count}')\n\nThe if check is only true for the values 95, 110 and 130, so exactly these\nthree lines are printed with fast. The variable count is increased by 1\nin exactly these three passes and has the value 3 at the end.\n\nExercise C (✩✩)\n\nComplete the code at the ___ places. Create a dictionary vehicle with the\ndetails of the test vehicle and print the sentence. The mass is 1200 kg.vehicle = {\n    'name': 'test_car_3',\n    'mass_kg': ___,\n    'year': 2021,\n}\n\nprint(f'{vehicle[___]} (year {vehicle[\"year\"]}) weighs '\n      f'{vehicle[\"mass_kg\"]} kg.')\n\n# code cell\n\n\n\nSolutionvehicle = {\n    'name': 'test_car_3',\n    'mass_kg': 1200,\n    'year': 2021,\n}\n\nprint(f'{vehicle[\"name\"]} (year {vehicle[\"year\"]}) weighs '\n      f'{vehicle[\"mass_kg\"]} kg.')\n\nThe first gap is the value 1200, the second one the key \"name\" in\nquotation marks. The output is test_car_3 (year 2021) weighs 1200 kg. An\nf-string may be spread over several lines, as long as each substring starts\nwith an f.\n\nExercise D (✩✩)\n\nComplete the function and the call at the ___ places. The function\nkmh_to_ms should convert a speed from km/h to m/s by dividing by 3.6. Then\nit is called in a loop for each measurement.def kmh_to_ms(speed_kmh):\n    return speed_kmh ___ 3.6\n\nspeeds_kmh = [72, 88, 95, 60, 110, 130]\n\nfor speed_kmh in speeds_kmh:\n    speed_ms = ___(speed_kmh)\n    print(f'{speed_kmh} km/h corresponds to {speed_ms:.1f} m/s')\n\n# code cell\n\n\n\nSolutiondef kmh_to_ms(speed_kmh):\n    return speed_kmh / 3.6\n\nspeeds_kmh = [72, 88, 95, 60, 110, 130]\n\nfor speed_kmh in speeds_kmh:\n    speed_ms = kmh_to_ms(speed_kmh)\n    print(f'{speed_kmh} km/h corresponds to {speed_ms:.1f} m/s')\n\nThe first gap is the division operator /, the second one the function name\nkmh_to_ms. Since the function is defined once, we can call it as often as we\nlike with different arguments, without writing out the conversion formula\neach time.\n\nExercise E (✩✩✩, mini-project)\n\nAn acceleration test delivers the measurement series\nspeeds_kmh = [72, 88, 95, 60, 110, 130] in km/h. Implement the following\nsteps.\n\nPart 1: Write a function\nkinetic_energy(speed_kmh, mass=1200) that first converts internally to m/s\nand then returns the kinetic energy in joules.\n\nPart 2: Iterate over speeds_kmh with a for loop. Compute the kinetic\nenergy for each measured value and keep the largest value computed so far in\na variable max_energy (hint: start with max_energy = 0 before the loop\nand compare in each pass with an if check).\n\nPart 3: Print the largest energy value found after the loop.\n\nClosing question: At which speed from the measurement series does this\nmaximum value occur, and why can we guess this even without running the code?\n\n# code cell\n\n\n\nSolutiondef kinetic_energy(speed_kmh, mass=1200):\n    speed_ms = speed_kmh / 3.6\n    return 0.5 * mass * speed_ms**2\n\nspeeds_kmh = [72, 88, 95, 60, 110, 130]\n\nmax_energy = 0\nfor speed_kmh in speeds_kmh:\n    energy = kinetic_energy(speed_kmh)\n    if energy > max_energy:\n        max_energy = energy\n\nprint(f'Maximum kinetic energy: {max_energy:.1f} joules')\n\nThe maximum value occurs at 130 km/h, the largest value in the measurement\nseries. Since the kinetic energy grows with the square of the speed and the\nmass stays constant over the entire measurement series, the maximum of the\nenergy is always at the highest measured speed. We can therefore derive this\nfrom the formula alone, without running the code.","type":"content","url":"/chapter01-sec04","position":1},{"hierarchy":{"lvl1":"1.5 Exercises"},"type":"lvl1","url":"/chapter01-sec05","position":0},{"hierarchy":{"lvl1":"1.5 Exercises"},"content":"These exercises are intended for self-study at home and review the material\nof chapters 1.1 to 1.4. Expect around two hours of working time.\n\nThe difficulty level is given in the title of each exercise:\n\n✩ Comprehension: predict and explain code and outputs (approx. 5 min)\n\n✩✩ Application: write your own code and interpret results (approx. 10 min)\n\n✩✩✩ Mini-project: combine several concepts of the part (approx. 30 min)\n\nExercise 1.1 (✩)\n\nWhich data type is present? Write your guess after the arrow before you run\nthe code.\n\n7 -->\n\n-7 -->\n\n'steel' -->\n\n7.0 -->\n\n7,0 -->\n\n7**2 -->\n\n7**(1/2) -->\n\n7 == 7.0 -->\n\nThen check each line with type() in a code cell.\n\n# code cell\n\n\n\nSolutionprint(type(7))\nprint(type(-7))\nprint(type('steel'))\nprint(type(7.0))\n\ncomma_number = 7,0\nprint(type(comma_number))\n\nprint(type(7**2))\nprint(type(7**(1/2)))\nprint(type(7 == 7.0))\n\n7 and -7 are integers, 'steel' is a string, 7.0 is a float. 7**2\nstays an integer (49), 7**(1/2) is a float (2.6457513110645907).\n7 == 7.0 is a bool with the value True, because Python looks at the\nnumeric value in a comparison, not the data type.\n\nThe most interesting case is 7,0: in Python the comma does not create a\ndecimal number but a tuple (7, 0), so type(comma_number) returns\ntuple. No error occurs in the process, which makes this typo particularly\ntreacherous. For decimal numbers, therefore, always use a point instead of a\ncomma.\n\nExercise 1.2 (✩✩)\n\nA spring obeys the spring law force = spring_constant * displacement.\nCompute the spring force for a spring constant of 250 N/m and a displacement\nof 0.12 m. Print the result with an f-string and the unit newton, rounded to\ntwo decimal places.\n\n# code cell\n\n\n\nSolutionspring_constant = 250   # N/m\ndisplacement = 0.12     # m\n\nforce = spring_constant * displacement\nprint(f'Spring force: {force:.2f} N')\n\nThe spring force is 30.00 N. Since spring_constant is an integer and\ndisplacement is a float, Python automatically converts the result of the\nmultiplication to a float.\n\nExercise 1.3 (✩)\n\nWhat do the following expressions return? Note True or False before you\nrun the code.\n\n5 > 3 -->\n\n5 >= 5 -->\n\n'Steel' == 'steel' -->\n\nnot (5 > 3) -->\n\n(5 > 3) and (2 > 4) -->\n\n(5 > 3) or (2 > 4) -->\n\n# code cell\n\n\n\nSolutionprint(5 > 3)\nprint(5 >= 5)\nprint('Steel' == 'steel')\nprint(not (5 > 3))\nprint((5 > 3) and (2 > 4))\nprint((5 > 3) or (2 > 4))\n\n5 > 3 and 5 >= 5 are True. 'Steel' == 'steel' is False, because\nPython distinguishes uppercase and lowercase when comparing strings.\nnot (5 > 3) inverts True to False. With and both sides must be true,\nso the result is False. With or one true side is enough, so the result\nis True.\n\nExercise 1.4 (✩✩)\n\nA material has a measured tensile strength of tensile_strength_mpa = 420.\nWrite an if/elif/else branch that prints the following categories:\n\nbelow 300 MPa: 'low-strength'\n\nfrom 300 to 600 MPa (both inclusive): 'medium-strength'\n\nabove 600 MPa: 'high-strength'\n\n# code cell\n\n\n\nSolutiontensile_strength_mpa = 420\n\nif tensile_strength_mpa < 300:\n    category = 'low-strength'\nelif tensile_strength_mpa <= 600:\n    category = 'medium-strength'\nelse:\n    category = 'high-strength'\n\nprint(f'Category: {category}')\n\nAt 420 MPa the material lies in the range from 300 to 600 MPa and is\ntherefore classified as 'medium-strength'.\n\nExercise 1.5 (✩✩)\n\nWrite a for loop that prints the value in Fahrenheit for the temperatures 0,\n20, 40, 60, 80 and 100 degrees Celsius. Use the formula\nfahrenheit = celsius * 9/5 + 32 for this.\n\nHint: range(start, stop, step) generates numbers with a step size other\nthan 1. The value stop itself no longer belongs to the range, so choose it\nsuitably larger.\n\n# code cell\n\n\n\nSolutionfor celsius in range(0, 101, 20):\n    fahrenheit = celsius * 9/5 + 32\n    print(f'{celsius} degrees Celsius corresponds to {fahrenheit:.1f} degrees Fahrenheit')\n\nrange(0, 101, 20) generates the values 0, 20, 40, 60, 80 and 100. Since the\nvalue 101 itself is no longer contained in the range, this choice covers all\nsix desired temperatures.\n\nExercise 1.6 (✩✩)\n\nA component cools down from 180 degrees Celsius by 12 degrees in each time\nstep. Write a for loop with range(15) that computes and prints the\ntemperature for each time step. Add an if check in the loop body that\nadditionally prints below 20 degrees as soon as the temperature falls below\nthe mark of 20 degrees Celsius.\n\n# code cell\n\n\n\nSolutionfor time_step in range(15):\n    temperature = 180 - time_step * 12\n    print(f'Time step {time_step}: {temperature} degrees Celsius')\n    if temperature < 20:\n        print('below 20 degrees')\n\nThe temperature drops in uniform steps from 180 to 12 degrees Celsius. At\ntime step 14 it is 12 degrees Celsius, below the 20-degree mark for the first\ntime; the additional message appears there. With range(15) we cover all\ntime steps up to the point where the temperature drops below the mark.\n\nExercise 1.7 (✩)\n\nThe list my_list = [15, 8, 23, 4, 16, 42] is given. Note down your guess\nfirst, before you run the code.\n\nmy_list[0] -->\n\nmy_list[2] -->\n\nmy_list[-1] -->\n\nmy_list[-3] -->\n\nlen(my_list) -->\n\n# code cell\n\n\n\nSolutionmy_list = [15, 8, 23, 4, 16, 42]\nprint(my_list[0])\nprint(my_list[2])\nprint(my_list[-1])\nprint(my_list[-3])\nprint(len(my_list))\n\nmy_list[0] is 15, the first element, my_list[2] is 23, the element at\nindex 2. my_list[-1] is 42, the last element. my_list[-3] is 4,\nbecause counting from the back, 42 is at -1, 16 at -2 and 4 at -3.\nlen(my_list) yields 6, the number of elements.\n\nExercise 1.8 (✩✩)\n\nThe list measurement_series = [15.2, 8.7, 23.1, 4.4, 16.9, 42.0] with force\nreadings in newtons is given. Determine the smallest and largest value of the\nlist with a for loop, without using the built-in functions min() and\nmax().\n\n# code cell\n\n\n\nSolutionmeasurement_series = [15.2, 8.7, 23.1, 4.4, 16.9, 42.0]\n\nminimum = measurement_series[0]\nmaximum = measurement_series[0]\n\nfor value in measurement_series:\n    if value < minimum:\n        minimum = value\n    if value > maximum:\n        maximum = value\n\nprint(f'Minimum: {minimum} N, maximum: {maximum} N')\n\nWe start minimum and maximum with the first list element and then compare\nevery further element. The minimum of the measurement series is 4.4 N, the\nmaximum is 42.0 N.\n\nExercise 1.9 (✩✩)\n\nCreate a dictionary part for a shaft with the following information:\n\ndesignation: 'Shaft_A1'\n\ndiameter_mm: 25.0\n\nmaterial: '42CrMo4'\n\nmax_torque_nm: 180\n\nThen print: “Shaft_A1 made of 42CrMo4 (diameter 25.0 mm) withstands a maximum\nof 180 Nm.”\n\n# code cell\n\n\n\nSolutionpart = {\n    'designation': 'Shaft_A1',\n    'diameter_mm': 25.0,\n    'material': '42CrMo4',\n    'max_torque_nm': 180\n}\n\nprint(f'{part[\"designation\"]} made of {part[\"material\"]} '\n      f'(diameter {part[\"diameter_mm\"]} mm) withstands a maximum of '\n      f'{part[\"max_torque_nm\"]} Nm.')\n\nEach value is accessed via the matching key. Here, a dictionary immediately\nshows which value stands for what, in contrast to a list with the same four\nvalues.\n\nExercise 1.10 (✩✩)\n\nWrite a function stress(force_n, cross_section_mm2=100) that computes and\nreturns the mechanical stress sigma = force_n / cross_section_mm2 in N/mm²\n(MPa). Give the function a docstring. Call the function once with\nforce_n = 5000 and the default cross section, and once with force_n = 5000\nand cross_section_mm2 = 50. Compare the two results in one sentence.\n\n# code cell\n\n\n\nSolutiondef stress(force_n, cross_section_mm2=100):\n    \"\"\"Computes the mechanical stress in MPa (N/mm²).\"\"\"\n    return force_n / cross_section_mm2\n\nprint(f'{stress(5000):.1f} MPa')\nprint(f'{stress(5000, cross_section_mm2=50):.1f} MPa')\n\nWith the default cross section of 100 mm² the stress is 50.0 MPa, with the\nsmaller cross section of 50 mm² the stress doubles to 100.0 MPa. The same\nforce is distributed over less area with a smaller cross section, which\nincreases the stress.\n\nExercise 1.11 (✩✩✩, mini-project)\n\nIn a tensile test, a sample with the constant cross section\ncross_section_mm2 = 19.6 is loaded step by step. The measured forces are\nforces_n = [1200, 3400, 5800, 7200, 8100, 6500]. Implement the following\nsteps.\n\nPart 1: Write a function stress(force_n, cross_section_mm2) that returns\nthe stress in MPa.\n\nPart 2: Create a dictionary test with the keys material ('S235JR'),\ncross_section_mm2 (19.6) and yield_strength_mpa (235.0).\n\nPart 3: Iterate over forces_n with a for loop. Compute the stress for\neach value with your function and print for each measured value whether the\nyield strength from the dictionary is exceeded ('In the elastic range' or\n'Yield strength exceeded'). Also keep track of the force at which the yield\nstrength is first exceeded.\n\nPart 4: Determine the maximum stress of the entire measurement series\nwithout max() and print it at the end.\n\nClosing question: At which force from the list is the yield strength first\nexceeded, and what does that mean physically for the material sample?\n\n# code cell\n\n\n\nSolutiondef stress(force_n, cross_section_mm2):\n    \"\"\"Computes the mechanical stress in MPa (N/mm²).\"\"\"\n    return force_n / cross_section_mm2\n\ntest = {\n    'material': 'S235JR',\n    'cross_section_mm2': 19.6,\n    'yield_strength_mpa': 235.0\n}\n\nforces_n = [1200, 3400, 5800, 7200, 8100, 6500]\n\nmax_stress = 0\nfirst_exceedance_force = 0\nexceedance_found = False\n\nfor force in forces_n:\n    sigma = stress(force, test['cross_section_mm2'])\n\n    if sigma > test['yield_strength_mpa']:\n        status = 'Yield strength exceeded'\n        if not exceedance_found:\n            first_exceedance_force = force\n            exceedance_found = True\n    else:\n        status = 'In the elastic range'\n\n    print(f'{force} N -> {sigma:.1f} MPa: {status}')\n\n    if sigma > max_stress:\n        max_stress = sigma\n\nprint(f'Maximum stress: {max_stress:.1f} MPa')\nprint(f'First exceedance at {first_exceedance_force} N')\n\nThe yield strength of 235.0 MPa is first exceeded at 5800 N, where the stress\nis about 295.9 MPa. The maximum stress of the entire measurement series\noccurs at the largest force of 8100 N and is about 413.3 MPa, because with a\nconstant cross section the stress grows proportionally to the force.\n\nPhysically, exceeding the yield strength means that from this loading point\nonward the material sample no longer deforms purely elastically, but\nplastically. The sample thus takes on a permanent deformation that does not\ndisappear even after complete unloading.","type":"content","url":"/chapter01-sec05","position":1},{"hierarchy":{"lvl1":"Python Cheat Sheet"},"type":"lvl1","url":"/chapter01-sec06","position":0},{"hierarchy":{"lvl1":"Python Cheat Sheet"},"content":"This page summarizes the Python building blocks from Part 1 at a glance. It\nis intended as a reference aid for the exercises and the exam.","type":"content","url":"/chapter01-sec06","position":1},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Variables and data types"},"type":"lvl2","url":"/chapter01-sec06#variables-and-data-types","position":2},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Variables and data types"},"content":"speed = 27.8          # float (decimal number, decimal point)\ncount = 50            # integer (whole number)\nname = 'Sensor_A'     # string (text in quotation marks)\n\ntype(speed)           # shows the data type: <class 'float'>\n\nAn assignment with = first computes the right-hand side and stores the\nresult in the variable on the left. count = count + 1 increases count by\n1.","type":"content","url":"/chapter01-sec06#variables-and-data-types","position":3},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Output with print() and f-strings"},"type":"lvl2","url":"/chapter01-sec06#output-with-print-and-f-strings","position":4},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Output with print() and f-strings"},"content":"print(speed)                      # simple output\nprint(f'Value: {speed} km/h')     # f-string: variable in { }\nprint(f'Value: {speed:.2f} km/h') # rounded to 2 decimal places","type":"content","url":"/chapter01-sec06#output-with-print-and-f-strings","position":5},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Operators"},"type":"lvl2","url":"/chapter01-sec06#operators","position":6},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Operators"},"content":"Kind\n\nOperators\n\nArithmetic\n\n+  -  *  /  ** (power)\n\nComparison\n\n>  <  >=  <=  == (equal)  != (not equal)\n\nLogic\n\nand  or  not\n\nA comparison always yields True or False. = is the assignment, == is\nthe comparison.","type":"content","url":"/chapter01-sec06#operators","position":7},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Branch: if / elif / else"},"type":"lvl2","url":"/chapter01-sec06#branch-if-elif-else","position":8},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Branch: if / elif / else"},"content":"if speed > 33.3:\n    print('too fast')\nelif speed == 33.3:\n    print('exactly at the limit')\nelse:\n    print('within the limit')\n\nThe header ends with :, the associated block is indented.","type":"content","url":"/chapter01-sec06#branch-if-elif-else","position":9},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Repetition: for loop"},"type":"lvl2","url":"/chapter01-sec06#repetition-for-loop","position":10},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Repetition: for loop"},"content":"for time_step in range(5):           # 0, 1, 2, 3, 4\n    print(time_step)\n\nfor value in measurement_series:     # directly over a list\n    print(value)\n\nrange(5) generates 0 to 4. range(0, 101, 20) generates 0, 20, 40, 60, 80,\n100.","type":"content","url":"/chapter01-sec06#repetition-for-loop","position":11},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"List"},"type":"lvl2","url":"/chapter01-sec06#list","position":12},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"List"},"content":"measurement_series = [80, 95, 120, 60, 110]\n\nmeasurement_series[0]          # first element: 80\nmeasurement_series[-1]         # last element: 110\nlen(measurement_series)        # number of elements: 5\nmeasurement_series.append(75)  # append a value at the end","type":"content","url":"/chapter01-sec06#list","position":13},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Dictionary"},"type":"lvl2","url":"/chapter01-sec06#dictionary","position":14},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Dictionary"},"content":"sensor = {\n    'name': 'Sensor_A',\n    'measuring_range_max': 200.0,\n}\n\nsensor['name']                       # access via the key: 'Sensor_A'\nsensor['measuring_range_max'] = 250.0   # change a value\nsensor['calibrated'] = True             # new key-value pair","type":"content","url":"/chapter01-sec06#dictionary","position":15},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Function"},"type":"lvl2","url":"/chapter01-sec06#function","position":16},{"hierarchy":{"lvl1":"Python Cheat Sheet","lvl2":"Function"},"content":"def kmh_to_ms(speed_kmh):\n    \"\"\"Converts km/h to m/s.\"\"\"       # docstring: short description\n    return speed_kmh / 3.6\n\nkmh_to_ms(95)                          # call with one argument\n\ndef kinetic_energy(v, mass=1200):     # mass has a default value\n    return 0.5 * mass * v ** 2\n\nkinetic_energy(27.8)                   # mass = 1200\nkinetic_energy(27.8, mass=1500)        # mass overridden\n\ndef defines the function, return returns a value. Without return the\nfunction yields None.","type":"content","url":"/chapter01-sec06#function","position":17},{"hierarchy":{"lvl1":"2.1 NumPy Basics"},"type":"lvl1","url":"/chapter02-sec01","position":0},{"hierarchy":{"lvl1":"2.1 NumPy Basics"},"content":"In measurement technology, thousands of measured values quickly accumulate.\nAn acceleration sensor monitoring a vibrating machine, for example, delivers\n10,000 measured values per second. If we want to process this data with\nPython lists, we need loops over thousands of elements: tedious to write and\nslow to run. In this chapter we introduce NumPy, a library built for exactly\nsuch tasks. Its central data type, the array, allows us to apply\nmathematical operations and statistical measures directly to whole number\nseries, without writing a single loop.","type":"content","url":"/chapter02-sec01","position":1},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Learning objectives"},"type":"lvl2","url":"/chapter02-sec01#learning-objectives","position":2},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Learning objectives"},"content":"Learning objectives\n\nYou know what a NumPy array is and how it differs from a Python\nlist.\n\nYou can create an array with np.array(), np.linspace() and\nnp.zeros().\n\nYou can apply vector operations (element-wise addition,\nmultiplication, scaling) to arrays.\n\nYou can apply mathematical functions such as np.sin() and np.exp()\nto arrays.\n\nYou can compute statistical measures of an array with np.mean(),\nnp.std(), np.min() and np.max().","type":"content","url":"/chapter02-sec01#learning-objectives","position":3},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"What is a NumPy array?"},"type":"lvl2","url":"/chapter02-sec01#what-is-a-numpy-array","position":4},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"What is a NumPy array?"},"content":"NumPy (short for Numerical Python) is a library, that is, a collection of\nready-made functions that we can use in our own code without writing them\nourselves. Before we can use a library, we have to load it with import. For\nNumPy a fixed abbreviation has become established, under which we refer to\nthe library in the rest of the code:\n\nimport numpy as np\n\n\n\nThis line loads the numpy library and makes it available in the code under\nthe name np. From now on we call all functions from NumPy with this\nabbreviation, for example np.array(). The abbreviation np is pure\nconvention; a different name would work just as well technically, but np is\nso widespread in the Python world that practically every piece of NumPy code\nuses it.\n\nThe central data type of NumPy is the array. It allows us to apply\nmathematical operations directly to whole number series, without writing a\nsingle loop, as we will see below.\n\nThe difference between a list and an array is seen most quickly with an\nexample. A sensor delivers five acceleration values in m/s^2:\n\n# accelerations in m/s^2 as a Python list\nmeasurements_list = [0.3, 1.2, 2.5, 1.8, 0.7]\n\n# accelerations in m/s^2 as a NumPy array\nmeasurements_array = np.array([0.3, 1.2, 2.5, 1.8, 0.7])\n\nprint(measurements_list)\nprint(measurements_array)\n\n\n\nAt first glance, a list and an array look similar. A NumPy array, however, is\ndesigned specifically for numerical data and mathematical calculations. This\nlets us apply arithmetic operations directly to whole measurement series.\nFirst, though, we look at further ways to create a NumPy array.\n\nBesides np.array(), which converts an existing list into an array, NumPy\nprovides two more functions with which we create arrays directly, without\nwriting down the values one by one.\n\nnp.linspace(start, stop, count) generates count evenly distributed values\nbetween start and stop. The end value stop is included by default. This\nis suitable, for example, for time axes:\n\nt = np.linspace(0, 2, 5)    # 5 values between 0 and 2 seconds\nprint(t)\n\n\n\nIn the output there is a point after each whole number, that is, 0. instead\nof 0. np.linspace() always generates the values as floating-point\nnumbers, even if we specify integer bounds. This is intentional, because the\nevenly distributed intermediate values of an axis are generally not whole\nnumbers.\n\nnp.zeros(count) generates an array of all zeros. This is useful for\ncreating an array as a placeholder that is filled with values later:\n\nplaceholder = np.zeros(5)   # placeholder with 5 zeros\nprint(placeholder)\n\n\n\nWith these three functions, np.array(), np.linspace() and np.zeros(),\nwe already cover most cases in which we need to create a new array: from\nexisting values, as an evenly distributed axis or as a placeholder.\n\nBefore we continue computing with the arrays we have created, we check their\nbasic properties: size and data type.\n\nprint(measurements_array.shape)   # number of elements per dimension\nprint(measurements_array.dtype)   # data type of the stored values\n\n\n\n.shape returns the dimensions of the array as a tuple. (5,) means: one\ndimension with five elements. .dtype returns the common data type of all\nelements, here typically float64 for floating-point numbers. These two\nattributes are the fastest way to inspect an unknown array.\n\nMini-exercise (✩)\n\nA temperature sensor delivers four measured values in °C: 18.5, 19.2,\n18.9, 20.1.\n\nStore the values in an array called temperatures.\n\nPrint the shape and data type of temperatures.\n\nCreate a time axis time with four evenly distributed values between 0\nand 3 seconds, without writing down the values one by one.\n\nCreate an array calibration_values with four zeros that is to serve\nlater as a placeholder for calibration factors.\n\nAnswer without running: which data type does time.dtype return, even\nthough you only specified the whole numbers 0 and 3 as bounds in\nnp.linspace()? Justify your answer.\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\ntemperatures = np.array([18.5, 19.2, 18.9, 20.1])\nprint(temperatures.shape)   # (4,)\nprint(temperatures.dtype)   # float64\n\ntime = np.linspace(0, 3, 4)\nprint(time)                 # [0. 1. 2. 3.]\n\ncalibration_values = np.zeros(4)\nprint(calibration_values)   # [0. 0. 0. 0.]\n\ntime.dtype returns float64. np.linspace() generates its values as\nfloating-point numbers by default, because the computed intermediate values\nof an axis are generally not integers. That is why NumPy also stores the\nincidentally integer values 0, 1, 2 and 3 as floats, recognizable by the\npoint in the output [0. 1. 2. 3.].","type":"content","url":"/chapter02-sec01#what-is-a-numpy-array","position":5},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Vector operations and mathematical functions"},"type":"lvl2","url":"/chapter02-sec01#vector-operations-and-mathematical-functions","position":6},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Vector operations and mathematical functions"},"content":"In the last section we created arrays and looked at their structure with\n.shape and .dtype. Now we see what really makes arrays useful:\narithmetic operations that act on whole number series, without writing a\nsingle loop.\n\nSuppose we want to compute the acting force from the acceleration values.\nF = m \\cdot a holds, where the mass is m = 5\\,\\mathrm{kg}. With a Python\nlist we need a loop for this:\n\n# with the list: a manual loop is necessary\nforces_list = []\nfor a in measurements_list:\n    forces_list.append(5.0 * a)\n\nprint(forces_list)\n\n\n\nWith the NumPy array a single line is enough:\n\nforces_array = 5.0 * measurements_array\nprint(forces_array)\n\n\n\nThe multiplication by the scalar 5.0 is automatically applied to every\nelement of measurements_array. We call such operations on whole number\nseries vector operations. They avoid explicit loops and are therefore\nusually considerably more efficient for large measurement series.\n\nIs this difference noticeable at all with five measured values? With five\nvalues, no. But as soon as a sensor delivers several thousand values per\nsecond, the vector operation determines whether an analysis takes fractions\nof a second or several minutes.\n\nThe same principle applies to the basic arithmetic operations +, -, *,\n/ and **. If we add two one-dimensional arrays of equal length, their\nelements are added pairwise:\n\nsensor_a = np.array([0.3, 1.2, 2.5, 1.8, 0.7])\nsensor_b = np.array([0.1, 0.2, 0.3, 0.1, 0.2])\n\ntotal = sensor_a + sensor_b\nprint(total)\n\n\n\nThe first element of sensor_a is added to the first element of sensor_b,\nthe second to the second, and so on. Since both arrays contain five elements,\nthis pairing of partners is unambiguous.\n\nFor one-dimensional arrays, a pairwise operation is normally only possible if\nboth arrays have the same length. For multi-dimensional arrays, the shapes\n(.shape) of the arrays must match each other. Under certain conditions,\nNumPy can also combine differently shaped arrays; these rules are called\nbroadcasting and are covered later.\n\nBesides the basic arithmetic operations, NumPy also provides mathematical\nfunctions that act element-wise on arrays. We need two of them often:\nnp.sin() for trigonometric calculations and np.exp() for exponential\nfunctions.\n\nangle = np.linspace(0, 2 * np.pi, 5)\nprint(np.sin(angle))\n\n\n\nnp.sin() applies the sine to each element of angle individually and\nreturns a new array of the same length. In the output it is noticeable that\nfor the angles \\pi and 2\\pi the value is not exactly 0, but a tiny\nvalue on the order of 1e-16. The exact sine would be zero at these points.\nThis small deviation is a rounding error of floating-point arithmetic\nthat we always have to reckon with in numerical calculations.\n\nnp.exp() works on the same principle:\n\nvalues = np.array([0.0, 1.0, 2.0, 3.0])\nprint(np.exp(values))\n\n\n\nPython already comes with math.sin() and math.exp() in the math module,\nbut these only accept single numbers, not arrays. To apply them to several\nvalues, we would again need a loop. The NumPy variants np.sin() and\nnp.exp() are built for arrays and are therefore the right choice in this\nlecture.\n\nMini-exercise (✩)\n\nOn a crane, two ropes pull with the following forces in kN, measured at four\npoints in time:rope_1 = np.array([120.0, 135.0, 128.0, 140.0])\nrope_2 = np.array([80.0, 75.0, 82.0, 78.0])\n\nCompute the sum of the magnitudes of both rope tension forces at each\npoint in time and store it in total_rope_forces_kn.\n\nConvert total_rope_forces_kn to newtons and store the result in\ntotal_rope_forces_newton (1 kN = 1000 N).\n\nRope 1 is inclined relative to the horizontal. The angle at the four\npoints in time is, in rad:   angle = np.array([0.50, 0.55, 0.52, 0.58])\n\nCompute the vertical component of the force in rope 1 and store it in\nrope_1_vertical.\n4. Answer without running: how many elements does rope_1 * np.sin(angle)\nhave, and which angle belongs to the third element of rope_1? Is the\nvertical component at each point in time larger or smaller than the total\nforce in the rope?\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\nrope_1 = np.array([120.0, 135.0, 128.0, 140.0])\nrope_2 = np.array([80.0, 75.0, 82.0, 78.0])\n\ntotal_rope_forces_kn = rope_1 + rope_2\nprint(total_rope_forces_kn)\n\ntotal_rope_forces_newton = total_rope_forces_kn * 1000.0\nprint(total_rope_forces_newton)\n\nangle = np.array([0.50, 0.55, 0.52, 0.58])\nrope_1_vertical = rope_1 * np.sin(angle)\nprint(rope_1_vertical)\n\nrope_1 * np.sin(angle) again has four elements, since NumPy multiplies the\ntwo arrays element-wise. The third element of rope_1 (128.0 kN) belongs to\nthe third angle (0.52 rad). The vertical component is smaller than the total\nforce in the rope at every point in time, because the sine yields values\nbetween 0 and 1 for angles between 0 and \\pi/2.","type":"content","url":"/chapter02-sec01#vector-operations-and-mathematical-functions","position":7},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Statistical measures"},"type":"lvl2","url":"/chapter02-sec01#statistical-measures","position":8},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Statistical measures"},"content":"So far we have looked at individual values of an array or transformed the\nwhole array at once. Often, however, we are not interested in every single\nvalue, but in a summarizing measure: how large is a measured value on\naverage? How much do the values fluctuate? NumPy provides functions for this\nthat compute a single number from an array.\n\nAs a data basis we take a measurement series: the peak acceleration that a\nsensor recorded during twelve consecutive test runs of the same machine.\n\npeak_values = np.array([4.8, 5.1, 4.6, 5.3, 4.9, 5.0,\n                        4.7, 5.2, 4.9, 5.4, 4.8, 5.0])\nprint(peak_values)\n\n\n\nWith the NumPy functions np.mean(), np.min() and np.max() we compute\nthe mean, minimum and maximum. np.mean() adds all values and divides by the\nnumber of elements, just like computing a mean by hand, only without a loop.\nnp.min() and np.max() yield the smallest and largest value in the array\nrespectively.\n\nprint(f\"Mean:    {np.mean(peak_values):.2f} m/s^2\")\nprint(f\"Minimum: {np.min(peak_values):.2f} m/s^2\")\nprint(f\"Maximum: {np.max(peak_values):.2f} m/s^2\")\n\n\n\nThe standard deviation describes how much the individual values deviate from\nthe mean on average. A small standard deviation means that the test runs\ndelivered very similar peak values. A large standard deviation shows that the\nmachine reacts noticeably differently from run to run. The standard deviation\nis computed with np.std().\n\nprint(f\"Standard deviation: {np.std(peak_values):.3f} m/s^2\")\n\n\n\nnp.mean(), np.std(), np.min() and np.max() can also be called\ndirectly as a method of the array: peak_values.mean() yields the same\nresult as np.mean(peak_values). Both notations are common. In these lecture\nnotes we consistently use the function notation np.function(array), because\nit works regardless of whether we are working with an array or an ordinary\nlist.\n\nWith these four functions, every measurement series can be characterized at a\nglance: a typical value through the mean, the scatter through the standard\ndeviation and the extreme values through minimum and maximum. These are the\nfirst tools for deriving reliable statements about a measured system from\npure number series.\n\nMini-exercise (✩)\n\nIn a quality inspection, the tightening torque of ten screws is measured, in\nNm:torques = np.array([45.2, 44.8, 46.1, 45.5, 44.9,\n                    45.8, 46.3, 44.6, 45.1, 45.9])\n\nDetermine the mean tightening torque and store it in mean_torque.\n\nDetermine how much the values scatter around this mean on average, and\nstore the result in spread.\n\nDetermine the smallest and the largest measured torque and store them in\nmin_torque and max_torque.\n\nCompute the range of the measurement (difference between the largest and\nsmallest value) from min_torque and max_torque and store it in\nspan.\n\nEstimate before running: is spread closer to 0.6 Nm or closer to 6 Nm?\nJustify with a look at the ten measured values.\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\ntorques = np.array([45.2, 44.8, 46.1, 45.5, 44.9,\n                    45.8, 46.3, 44.6, 45.1, 45.9])\n\nmean_torque = np.mean(torques)\nprint(f\"Mean:    {mean_torque:.2f} Nm\")\n\nspread = np.std(torques)\nprint(f\"Spread:  {spread:.3f} Nm\")\n\nmin_torque = np.min(torques)\nmax_torque = np.max(torques)\nprint(f\"Minimum: {min_torque:.2f} Nm\")\nprint(f\"Maximum: {max_torque:.2f} Nm\")\n\nspan = max_torque - min_torque\nprint(f\"Span:    {span:.2f} Nm\")\n\nAll measured values lie close together between 44.6 Nm and 46.3 Nm, that is,\nin a band only about 1.7 Nm wide. The standard deviation measures the average\ndeviation from the mean and is therefore considerably smaller than this\nbandwidth, here about 0.6 Nm. A value of 6 Nm would be impossible, since no\nsingle deviation is that large.","type":"content","url":"/chapter02-sec01#statistical-measures","position":9},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Summary and outlook"},"type":"lvl2","url":"/chapter02-sec01#summary-and-outlook","position":10},{"hierarchy":{"lvl1":"2.1 NumPy Basics","lvl2":"Summary and outlook"},"content":"We have met NumPy arrays as an alternative to Python lists, created them with\nnp.array(), np.linspace() and np.zeros() and examined them with\n.shape and .dtype. We apply vector operations and functions such as\nnp.sin() and np.exp() directly to whole arrays, without writing loops.\nWith np.mean(), np.std(), np.min() and np.max() we summarize a\nmeasurement series in a few key figures.\n\nIn the next chapter we apply these tools in a coherent project and analyze\nthe test-rig run of a wind turbine. After that we introduce Matplotlib in\norder to plot whole measurement series. Two-dimensional arrays and systems of\nlinear equations follow in chapter 3.","type":"content","url":"/chapter02-sec01#summary-and-outlook","position":11},{"hierarchy":{"lvl1":"2.2 Test Rig for a Wind Turbine"},"type":"lvl1","url":"/chapter02-sec02","position":0},{"hierarchy":{"lvl1":"2.2 Test Rig for a Wind Turbine"},"content":"In chapter 2.1 we created NumPy arrays, processed them with vector operations\nand functions such as np.exp() and summarized them into key figures with\nnp.mean() and np.std(). In this chapter we apply these tools to a\ncoherent case: the analysis of a test-rig run for a wind turbine. Work\nthrough the sub-tasks in pairs if possible and in order, because each part\nbuilds on the results of the previous one.\n\nProject: Test-rig run of a wind turbine (✩✩)\n\nA test run measures the wind speed at eight points in time during a 70-second\nstart-up phase (in m/s):3.2, 5.1, 6.8, 7.5, 6.2, 4.9, 5.5, 6.0\n\nFrom these measured values we determine step by step the electrical power\nthat the generator delivers, and then characterize the test run\nstatistically.\n\nPart 1: Create data and time axis\n\nCreate the wind speeds as an array wind_speed. Also create a time axis\ntime with eight evenly distributed values between 0 and 70 s, without\nwriting down the values one by one. Print the shape and data type of both\narrays.\n\n# code cell\n\n\n\nSolution Part 1import numpy as np\n\nwind_speed = np.array([3.2, 5.1, 6.8, 7.5, 6.2, 4.9, 5.5, 6.0])\ntime = np.linspace(0, 70, 8)\n\nprint(wind_speed.shape, wind_speed.dtype)\nprint(time.shape, time.dtype)\n\nBoth arrays have the shape (8,) and the data type float64.\nnp.linspace(0, 70, 8) generates the points in time 0, 10, 20 up to 70 s, so\nthe interval between two measurements is 10 s.\n\nPart 2: Compute the rotor power\n\nThe rotor power can be computed in simplified form as P = k \\cdot v^3, with\nk = 1.2. Compute rotor_power in watts from wind_speed.\n\n# code cell\n\n\n\nSolution Part 2k = 1.2\nrotor_power = k * wind_speed**3\nprint(rotor_power)\n\nThe power **3 and the multiplication by k act element-wise on the whole\narray. Because the power grows with the third power of the wind speed, the\nfastest measured value (7.5 m/s) already delivers about thirteen times as\nmuch (around 506 W) as the slowest (3.2 m/s, around 39 W).\n\nPart 3: Efficiency of the generator\n\nThe generator needs a start-up time to reach its full efficiency. The\nefficiency at time t follows approximately\\eta(t) = \\eta_{max} \\cdot \\left(1 - e^{-t/\\tau}\\right)\n\nwith \\eta_{max} = 0.95 and \\tau = 20\\,\\text{s}. Compute efficiency for\nthe points in time from time.\n\n# code cell\n\n\n\nSolution Part 3eta_max = 0.95\ntau = 20.0\nefficiency = eta_max * (1 - np.exp(-time / tau))\nprint(efficiency)\n\nnp.exp() acts element-wise on -time / tau. At time 0 s the efficiency is\n0, after which it approaches the limit value 0.95. After 70 s, that is, three\nand a half time constants, about 97 percent of this limit value is reached.\n\nPart 4: Electrical power\n\nCompute the electrical power actually delivered, electrical_power, from\nrotor_power and efficiency.\n\n# code cell\n\n\n\nSolution Part 4electrical_power = rotor_power * efficiency\nprint(electrical_power)\n\nBoth arrays have eight elements, so NumPy multiplies them pairwise: at each\npoint in time the rotor power is weighted by the corresponding efficiency. At\nthe first point in time the result is 0 W, because the generator has not yet\nstarted up.\n\nPart 5: Characterize the test run\n\nDetermine the mean, minimum and maximum delivered power, as well as the\nscatter of the power around the mean.\n\n# code cell\n\n\n\nSolution Part 5mean_power = np.mean(electrical_power)\nmin_power = np.min(electrical_power)\nmax_power = np.max(electrical_power)\nspread = np.std(electrical_power)\n\nprint(f\"Mean:    {mean_power:.1f} W\")\nprint(f\"Minimum: {min_power:.1f} W\")\nprint(f\"Maximum: {max_power:.1f} W\")\nprint(f\"Spread:  {spread:.1f} W\")\n\nThe minimum of 0 W comes from the first point in time, at which the generator\nis still at rest. The scatter is of the same order of magnitude as the mean,\nso the test run delivers a very uneven power. This is due both to the\nstart-up phase of the generator and to the fluctuating wind speed.\n\nClosing question\n\nAnswer in your own words, without further code:\n\nThe wind speed in the test run only fluctuates between about 3 and 8 m/s.\nWhy does the rotor power nevertheless fluctuate so much more strongly?\n\nWhat does the large scatter of the electrical power mean for the operation\nof the turbine? Name one practical consequence.\n\nSolution closing question\n\nThe rotor power grows with the third power of the wind speed\n(P = k \\cdot v^3). Through the third power, the ratio of about 8 to 3\nbetween the two wind speeds becomes a ratio of about 19 to 1 in the rotor\npower. Even moderate changes in wind speed therefore have a\ndisproportionate effect on the power.\n\nA strongly fluctuating electrical power stresses the power grid and the\npower electronics of the turbine. In practice, the turbine must cushion\nthe fluctuations, for example by controlling the rotor blades, by buffer\nstorage or by feeding into the grid only above a minimum wind speed.\n\nBonus exercise: Second site (✩✩✩)\n\nA second test run at a windier site delivers the following wind speeds (m/s)\nat the same eight points in time:7.5, 8.1, 6.9, 9.2, 8.8, 7.6, 8.4, 9.0\n\nCarry out the calculation from Part 1 to Part 5 for this second site. The\ntime axis and the efficiency curve depend only on the start-up time, not on\nthe site, and therefore remain unchanged. Which site delivers more power on\naverage? Which delivers the more even power relative to the mean?\n\n# code cell\n\n\n\nSolution bonus exercisewind_speed_2 = np.array([7.5, 8.1, 6.9, 9.2, 8.8, 7.6, 8.4, 9.0])\n\n# k and efficiency come from Part 2 and Part 3 and apply unchanged.\nrotor_power_2 = k * wind_speed_2**3\nelectrical_power_2 = rotor_power_2 * efficiency\n\nmean_power_2 = np.mean(electrical_power_2)\nspread_2 = np.std(electrical_power_2)\n\nprint(f\"Site 1: {mean_power:.1f} W (spread {spread:.1f} W)\")\nprint(f\"Site 2: {mean_power_2:.1f} W (spread {spread_2:.1f} W)\")\n\nprint(f\"Spread/mean site 1: {spread / mean_power:.2f}\")\nprint(f\"Spread/mean site 2: {spread_2 / mean_power_2:.2f}\")\n\nSite 2 delivers a considerably higher mean power, because the wind speeds are\nconsistently higher and the power grows with the third power. The absolute\nscatter is also larger at site 2. More meaningful is the ratio of scatter to\nmean: this value is smaller at site 2, so the power there is more even in\nrelative terms.","type":"content","url":"/chapter02-sec02","position":1},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib"},"type":"lvl1","url":"/chapter02-sec03","position":0},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib"},"content":"In chapters 2.1 and 2.2 we computed measurement series as NumPy arrays and\nprinted them with print(). A column of two hundred numbers, however, tells\nus little about how a quantity behaves. On the test rig of an electric motor,\nfor example, we want to see at a glance how torque, power and efficiency\ndepend on the rotational speed. In this chapter we introduce Matplotlib,\nthe standard library for diagrams in Python, and turn our number material\ninto meaningful graphics.\n\nAs a running example, we follow a test-rig run in which the rotational speed\nof an electric motor is slowly ramped up from zero to the no-load speed. From\nthe rotational speed we compute the remaining characteristic quantities and\nplot them step by step.","type":"content","url":"/chapter02-sec03","position":1},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Learning objectives"},"type":"lvl2","url":"/chapter02-sec03#learning-objectives","position":2},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Learning objectives"},"content":"Learning objectives\n\nYou know the relationship between Figure and Axes and create\nboth with plt.subplots().\n\nYou create line plots with ax.plot(), draw several curves in one\ndiagram and add axis labels, title, legend and grid.\n\nYou save a diagram with fig.savefig().\n\nYou create several drawing areas with plt.subplots(nrows, ncols),\naddress them via ax[i] and align them with plt.tight_layout().\n\nYou plot measured points with ax.scatter() and add measurement\nuncertainties with ax.errorbar().","type":"content","url":"/chapter02-sec03#learning-objectives","position":3},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Drawing a characteristic curve as a line plot"},"type":"lvl2","url":"/chapter02-sec03#drawing-a-characteristic-curve-as-a-line-plot","position":4},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Drawing a characteristic curve as a line plot"},"content":"We start straight away with an example. From the rotational speed we compute\nthe torque of the motor. A simple model assumes that the torque decreases\nlinearly with the rotational speed: at standstill it is largest, at no-load\nspeed it drops to zero.\n\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\nrpm = np.linspace(0, 3000, 200)      # 1/min\nno_load_rpm = 3000.0\ntorque_standstill = 8.0              # Nm at standstill\n\ntorque = torque_standstill * (1 - rpm / no_load_rpm)\n\nfig, ax = plt.subplots()\nax.plot(rpm, torque)\nplt.show()\n\n\n\nThe lines with import load NumPy and Matplotlib. import matplotlib.pyplot as plt is the fixed convention for accessing Matplotlib, just like np for\nNumPy. The two lines with style are optional and only make the diagrams\nlook a little more appealing.\n\nplt.subplots() creates two objects. The Figure is the entire\nillustration, that is, the window or the image file. The Axes is the\ndrawing area within it, with an x-axis, y-axis and all curves. The pattern\nfig, ax = plt.subplots() stands at the beginning of every diagram in these\nlecture notes.\n\nax.plot(rpm, torque) draws a line through the points (rpm[0], torque[0]),\n(rpm[1], torque[1]) and so on. The first argument is always the x-axis, the\nsecond the y-axis. plt.show() displays the finished diagram.\n\nA diagram without labeled axes is worthless in engineering practice. We\ntherefore add axis labels with units, a title and a grid.\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.plot(rpm, torque)\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Torque in Nm')\nax.set_title('Torque characteristic of the electric motor')\nax.grid(True)\n\nplt.show()\n\n\n\nfigsize=(7, 4) sets the width and height of the Figure in inches.\nset_xlabel() and set_ylabel() label the axes, set_title() sets a title\nand grid(True) places a grid over the drawing area.\n\nOften we want to compare several curves. For this we simply call ax.plot()\nseveral times. We compare our motor with a weaker variant that delivers only\n6 Nm at standstill. Each curve gets a label that then appears in the\nlegend.\n\ntorque_weak = 6.0 * (1 - rpm / no_load_rpm)\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.plot(rpm, torque, label='Standard motor (8 Nm)')\nax.plot(rpm, torque_weak, linestyle='dashed',\n        label='Weak variant (6 Nm)')\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Torque in Nm')\nax.set_title('Two motor variants compared')\nax.legend()\nax.grid(True)\n\nplt.show()\n\n\n\nMatplotlib automatically gives each curve its own color. With linestyle we\nchange the line style; possible values are 'solid', 'dashed', 'dotted'\nand 'dashdot'. The call ax.legend() without arguments collects all\nlabel entries and shows them as a legend. This is cleaner than passing a\nlist of texts to ax.legend(), because the label is written directly at the\nrespective ax.plot() call.\n\nIn practice we want not only to look at a diagram, but also to insert it into\na report. fig.savefig() saves the Figure as a file. We call it in the same\ncode cell in which we create the diagram, directly before plt.show().\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.plot(rpm, torque, label='Standard motor (8 Nm)')\nax.plot(rpm, torque_weak, linestyle='dashed',\n        label='Weak variant (6 Nm)')\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Torque in Nm')\nax.set_title('Two motor variants compared')\nax.legend()\nax.grid(True)\n\nfig.savefig('torque_curve.png', dpi=150, bbox_inches='tight')\nplt.show()\n\n\n\ndpi=150 sets the resolution; for reports, 150 to 300 dpi are common.\nbbox_inches='tight' prevents labels at the edge from being cut off.\nMatplotlib recognizes the file format from the extension, .png for raster\ngraphics, .pdf or .svg for vector graphics.\n\nMini-exercise (✩)\n\nDraw the angular velocity of the motor over the rotational speed.\nangular_velocity = 2 * np.pi * rpm / 60 in rad/s holds.\n\nCreate a line plot with the rotational speed on the x-axis and the angular\nvelocity on the y-axis.\n\nLabel both axes with units and assign a title.\n\nAdd a grid.\n\nAnswer without code: you call ax.plot() once and then ax.legend(),\nwithout having set a label in the plot() call. What does the legend\nthen show?\n\n# code cell\n\n\n\nSolutionangular_velocity = 2 * np.pi * rpm / 60\n\nfig, ax = plt.subplots(figsize=(7, 4))\nax.plot(rpm, angular_velocity)\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Angular velocity in rad/s')\nax.set_title('Angular velocity of the electric motor')\nax.grid(True)\nplt.show()\n\nThe angular velocity rises linearly with the rotational speed; at 3000 1/min\nit reaches about 314 rad/s. If we call ax.legend() without having set a\nlabel, the legend stays empty and Matplotlib additionally prints a warning.\nA legend only makes sense if at least one curve has a label.","type":"content","url":"/chapter02-sec03#drawing-a-characteristic-curve-as-a-line-plot","position":5},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Showing torque, power and efficiency together"},"type":"lvl2","url":"/chapter02-sec03#showing-torque-power-and-efficiency-together","position":6},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Showing torque, power and efficiency together"},"content":"From the torque and angular velocity we compute the delivered power, and for\nthe efficiency we use a model that has its maximum near the rated speed.\n\nangular_velocity = 2 * np.pi * rpm / 60\npower = torque * angular_velocity\nefficiency = 0.9 * np.exp(-((rpm - 2200) / 900)**2)\n\n\n\nWhy don’t we simply draw these three quantities in a single diagram?\nBecause they have completely different value ranges and units: the torque\nlies between 0 and 8 Nm, the power at a few hundred watts and the efficiency\nbetween 0 and 1. In a common coordinate system, the torque and efficiency\nwould no longer be recognizable as flat lines at the bottom edge. The\nsolution is subplots: several drawing areas in a common Figure.\n\nfig, ax = plt.subplots(nrows=3, ncols=1, figsize=(7, 8))\n\nax[0].plot(rpm, torque)\nax[0].set_ylabel('Torque in Nm')\n\nax[1].plot(rpm, power)\nax[1].set_ylabel('Power in W')\n\nax[2].plot(rpm, efficiency)\nax[2].set_ylabel('Efficiency')\n\nfor single_axis in ax:\n    single_axis.set_xlabel('Rotational speed in 1/min')\n    single_axis.grid(True)\n\nax[0].set_title('Characteristic map of the electric motor')\n\nplt.tight_layout()\nplt.show()\n\n\n\nplt.subplots(nrows=3, ncols=1) creates three drawing areas one below the\nother and returns them as an array ax. We address the top one with ax[0],\nthe middle one with ax[1] and the bottom one with ax[2], just as with a\nNumPy array.\n\nFor the x-axis label and the grid we write a for loop over ax, because\nthese two settings are the same for all three subplots. The loop variable\nsingle_axis is one of the three drawing areas on each pass. Everything that\ndiffers between the subplots, that is, the y-label and the title, we set\nindividually via ax[0], ax[1] and ax[2].\n\nplt.tight_layout() increases the spacing between the subplots so that\nlabels do not overlap. We always call it directly before plt.show().\n\nSometimes we want to set the visible range of an axis instead of leaving it\nto Matplotlib. For the efficiency, the natural range is 0 to 1; we make this\nvisible with set_ylim().\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.plot(rpm, efficiency)\nax.set_ylim(0, 1)\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Efficiency with a fixed y-axis from 0 to 1')\nax.grid(True)\n\nplt.show()\n\n\n\nWithout set_ylim(), Matplotlib automatically scales the y-axis to the\nactual value range of the data, here about 0 to 0.9. With set_ylim(0, 1) we\nforce the full scale and immediately see how much distance still remains to\nthe ideal efficiency of 1.\n\nMini-exercise (✩)\n\nCreate two subplots side by side (nrows=1, ncols=2).\n\nLeft: power over the rotational speed.\n\nRight: efficiency over the rotational speed, with set_ylim(0, 1).\n\nLabel both subplots with axes and title, add a grid and align the Figure\nwith plt.tight_layout().\n\nAnswer without further code: at which rotational speed is the power\nmaximal? Is that the same rotational speed at which the efficiency is\nmaximal? Justify with a look at the two curves.\n\n# code cell\n\n\n\nSolutionfig, ax = plt.subplots(nrows=1, ncols=2, figsize=(12, 4))\n\nax[0].plot(rpm, power)\nax[0].set_xlabel('Rotational speed in 1/min')\nax[0].set_ylabel('Power in W')\nax[0].set_title('Power')\nax[0].grid(True)\n\nax[1].plot(rpm, efficiency)\nax[1].set_ylim(0, 1)\nax[1].set_xlabel('Rotational speed in 1/min')\nax[1].set_ylabel('Efficiency')\nax[1].set_title('Efficiency')\nax[1].grid(True)\n\nplt.tight_layout()\nplt.show()\n\nThe power is maximal at about 1500 1/min, that is, at half the no-load speed.\nThere the product of the still fairly high torque and the already fairly high\nangular velocity is largest. The efficiency reaches its maximum only at about\n2200 1/min. A motor therefore does not deliver its highest power at the\nrotational speed at which it works most efficiently.","type":"content","url":"/chapter02-sec03#showing-torque-power-and-efficiency-together","position":7},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Measured points with scatter and uncertainty"},"type":"lvl2","url":"/chapter02-sec03#measured-points-with-scatter-and-uncertainty","position":8},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Measured points with scatter and uncertainty"},"content":"So far we have drawn computed curves. On the real test rig we measure the\nefficiency only at individual rotational speeds, and each measurement\nscatters. We do not connect such individual measured values with a line, but\nplot them as individual points. This is called a scatter plot, created\nwith ax.scatter().\n\nmeasured_rpm = np.array([300, 600, 900, 1200, 1500, 1800, 2100, 2400, 2700])\nmeasured_efficiency = np.array([0.02, 0.05, 0.10, 0.28, 0.47, 0.75, 0.87, 0.88, 0.55])\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.scatter(measured_rpm, measured_efficiency)\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Measured efficiency at nine rotational speeds')\nax.grid(True)\n\nplt.show()\n\n\n\nLike ax.plot(), ax.scatter() expects the x-values first, then the\ny-values, but draws only points without a connecting line. This is exactly\nright when no continuous curve was measured between the measured points.\n\nEach measured point is the mean of several repeat measurements and has an\nuncertainty. We represent this with error bars. The appropriate function\nis ax.errorbar().\n\nmeasurement_uncertainty = np.array([0.02, 0.02, 0.03, 0.03, 0.04, 0.03, 0.02, 0.03, 0.05])\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.errorbar(measured_rpm, measured_efficiency, yerr=measurement_uncertainty,\n            fmt='o', capsize=4)\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Measured efficiency with measurement uncertainty')\nax.grid(True)\n\nplt.show()\n\n\n\nyerr passes the uncertainty in the y-direction as an array. fmt='o' draws\nthe measured points as circles, capsize=4 gives the caps at the ends of the\nerror bars a width of 4 points so that they are easier to read.\n\nFinally, we compare the measurement with our model from the last section. We\ndraw the measured points with error bars and the model curve in the same\ndiagram.\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.errorbar(measured_rpm, measured_efficiency, yerr=measurement_uncertainty,\n            fmt='o', capsize=4, label='Measurement')\nax.plot(rpm, efficiency, label='Model')\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Measurement and model compared')\nax.legend()\nax.grid(True)\n\nplt.show()\n\n\n\nMeasured points as symbols, model or regression as a solid line: this\npattern appears in almost every quantitative engineering report. The model\ncurve runs through almost all error bars, so the model describes the\nmeasurement well. Only the point at 2700 1/min lies clearly below the curve;\na closer look is worthwhile there.\n\nMini-exercise (✩)\n\nOn the same test rig, the torque is measured at five rotational speeds:measured_rpm_torque = np.array([500, 1000, 1500, 2000, 2500])\nmeasured_torque = np.array([6.5, 5.6, 3.9, 2.8, 1.5])\nmeasurement_uncertainty_torque = np.array([0.3, 0.4, 0.3, 0.5, 0.4])\n\nPlot the measured points with ax.errorbar() and error bars in the\ny-direction.\n\nDraw the model curve torque over rpm in the same diagram.\n\nLabel the axes, assign a title and a legend.\n\nAnswer without code: why do we plot the five measured points with\nax.errorbar() instead of ax.plot()?\n\n# code cell\n\n\n\nSolutionmeasured_rpm_torque = np.array([500, 1000, 1500, 2000, 2500])\nmeasured_torque = np.array([6.5, 5.6, 3.9, 2.8, 1.5])\nmeasurement_uncertainty_torque = np.array([0.3, 0.4, 0.3, 0.5, 0.4])\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.errorbar(measured_rpm_torque, measured_torque, yerr=measurement_uncertainty_torque,\n            fmt='o', capsize=4, label='Measurement')\nax.plot(rpm, torque, label='Model')\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Torque in Nm')\nax.set_title('Measured and modeled torque')\nax.legend()\nax.grid(True)\n\nplt.show()\n\nThe model line runs through all error bars, so the linear assumption for the\ntorque fits the measurement. We use ax.errorbar() instead of ax.plot()\nbecause nothing was measured between the five rotational speeds. A connecting\nline would feign a continuous measured curve that does not exist. The model\ncurve, on the other hand, is actually computed for every rotational speed and\nmay be drawn as a line.","type":"content","url":"/chapter02-sec03#measured-points-with-scatter-and-uncertainty","position":9},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Summary and outlook"},"type":"lvl2","url":"/chapter02-sec03#summary-and-outlook","position":10},{"hierarchy":{"lvl1":"2.3 Diagrams with Matplotlib","lvl2":"Summary and outlook"},"content":"We have met Matplotlib as a tool for visualizing measurement data. The\npattern fig, ax = plt.subplots() stands at the beginning of every diagram. With ax.plot() we draw lines, with set_xlabel(), set_ylabel(),\nset_title(), legend() and grid() we label and design the diagram, with\nfig.savefig() we save it. We create several drawing areas with\nplt.subplots(nrows, ncols) and address them via ax[i];\nplt.tight_layout() aligns them neatly. We plot individual measured points\nwith ax.scatter(), their uncertainty with ax.errorbar().\n\nIn the next chapter we apply NumPy and Matplotlib together in a coherent\nproject and analyze the measurement data of a complete test-rig run.","type":"content","url":"/chapter02-sec03#summary-and-outlook","position":11},{"hierarchy":{"lvl1":"2.4 Test-rig Report for an Electric Motor"},"type":"lvl1","url":"/chapter02-sec04","position":0},{"hierarchy":{"lvl1":"2.4 Test-rig Report for an Electric Motor"},"content":"In chapters 2.1 to 2.3 we computed NumPy arrays, analyzed them statistically\nand plotted them with Matplotlib. In this chapter we apply these tools\ntogether and analyze a complete test-rig run of the electric motor from\nchapter 2.3. Work through the sub-tasks in pairs if possible and in order,\nbecause each part builds on the results of the previous one.\n\nProject: Test-rig report for the electric motor (✩✩)\n\nOn the test rig, the rotational speed of the motor was increased in twelve\nsteps from 250 to 3000 1/min. At each step, the torque at the shaft and the\nelectrical power input were measured. The measured values are already stored\nas arrays in the first code cell. From them we create the figures and key\nfigures for the test-rig report.\n\nPart 1: Read in the measurement data and compute characteristic quantities\n\nIn the code cell, the measured values are already stored as arrays rpm,\ntorque and power_input.\n\nCheck with .shape that all three arrays are the same length.\n\nCompute the angular velocity omega = 2 * np.pi * rpm / 60 in rad/s.\n\nCompute the mechanical power p_mech = torque * omega in watts.\n\nCompute the efficiency efficiency = p_mech / power_input and print the\narray.\n\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\nrpm = np.array([250, 500, 750, 1000, 1250, 1500,\n                1750, 2000, 2250, 2500, 2750, 3000])           # 1/min\ntorque = np.array([7.4, 6.7, 6.1, 5.4, 4.7, 4.1,\n                   3.4, 2.9, 2.3, 1.7, 1.1, 0.5])              # Nm\npower_input = np.array([1350, 1660, 1720, 1520, 1230, 980,\n                        800, 720, 660, 600, 560, 470])         # W\n\n# Add: check shapes, compute omega, p_mech and efficiency\n\n\n\nSolution Part 1print(rpm.shape, torque.shape, power_input.shape)\n\nomega = 2 * np.pi * rpm / 60\np_mech = torque * omega\nefficiency = p_mech / power_input\n\nprint(efficiency)\n\nAll three arrays have the shape (12,). The three calculations are vector\noperations and act on the whole array. The efficiency lies between about 0.14\nand 0.84 and is thus everywhere smaller than 1, as it must be for a real\nmotor. It first rises with the rotational speed, reaches a maximum and then\nfalls again.\n\nPart 2: Characteristic map as subplots\n\nCreate a Figure with three subplots one below the other (nrows=3). Show the\ntorque at the top, the mechanical power in the middle and the efficiency at\nthe bottom, each over the rotational speed. Label each subplot with its\ny-axis and give the efficiency subplot the full scale with set_ylim(0, 1).\nSet the x-axis label and the grid via a for loop over ax. The top subplot\ngets a title. Align the Figure with plt.tight_layout().\n\n# code cell\n\n\n\nSolution Part 2fig, ax = plt.subplots(nrows=3, ncols=1, figsize=(7, 8))\n\nax[0].plot(rpm, torque)\nax[0].set_ylabel('Torque in Nm')\n\nax[1].plot(rpm, p_mech)\nax[1].set_ylabel('Mechanical power in W')\n\nax[2].plot(rpm, efficiency)\nax[2].set_ylabel('Efficiency')\nax[2].set_ylim(0, 1)\n\nfor single_axis in ax:\n    single_axis.set_xlabel('Rotational speed in 1/min')\n    single_axis.grid(True)\n\nax[0].set_title('Characteristic map from the test-rig run')\n\nplt.tight_layout()\nplt.show()\n\nThis is the first time we see the three measured quantities in context. We\ntreat the measured points as a characteristic curve and connect them with a\nline to show the trend. The torque falls approximately linearly, the\nmechanical power has its maximum in the middle of the rotational-speed range,\nand the efficiency only becomes good at high rotational speed.\n\nPart 3: Determine operating points\n\nThe function np.argmax(array) returns the index of the largest value in the\narray. With rpm[np.argmax(p_mech)] you therefore find the rotational speed\nat which the mechanical power is largest.\n\nDetermine the rotational speed at which the mechanical power is maximal.\n\nDetermine the rotational speed at which the efficiency is maximal.\n\nCompute the mean, standard deviation and maximum of the mechanical power\nover the entire run.\n\nPrint the results as a short text report with f-strings.\n\n# code cell\n\n\n\nSolution Part 3rpm_max_power = rpm[np.argmax(p_mech)]\nrpm_max_efficiency = rpm[np.argmax(efficiency)]\n\nmean_power = np.mean(p_mech)\npower_spread = np.std(p_mech)\nmax_power = np.max(p_mech)\n\nprint(f\"Largest mechanical power at {rpm_max_power} 1/min\")\nprint(f\"Best efficiency at {rpm_max_efficiency} 1/min\")\nprint(f\"Mechanical power: mean {mean_power:.0f} W, \"\n      f\"spread {power_spread:.0f} W, maximum {max_power:.0f} W\")\n\nThe mechanical power is largest at about 644 W at 1500 1/min, the efficiency\nat 0.84 only at 2000 1/min. The motor therefore does not deliver its highest\npower at the rotational speed at which it has the lowest losses.\n\nPart 4: Efficiency with measurement uncertainty\n\nEach efficiency value is the mean of three repeat measurements. The standard\ndeviation of these repeats is given:efficiency_std = np.array([0.015, 0.018, 0.020, 0.022, 0.025, 0.024,\n                           0.022, 0.020, 0.019, 0.018, 0.028, 0.030])\n\nPlot the efficiency over the rotational speed with ax.errorbar() and\nerror bars in the y-direction (fmt='o', capsize=4).\n\nHighlight the operating point with the best efficiency by drawing it with\na second ax.scatter() call as a large star (marker='*', s=200).\n\nLabel the axes and title and show a legend.\n\n# code cell\n\n\n\nSolution Part 4efficiency_std = np.array([0.015, 0.018, 0.020, 0.022, 0.025, 0.024,\n                           0.022, 0.020, 0.019, 0.018, 0.028, 0.030])\n\ni_best = np.argmax(efficiency)\n\nfig, ax = plt.subplots(figsize=(7, 4))\n\nax.errorbar(rpm, efficiency, yerr=efficiency_std,\n            fmt='o', capsize=4, label='Measurement')\nax.scatter(rpm[i_best], efficiency[i_best],\n           marker='*', s=200, color='red', zorder=5, label='Best operating point')\n\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Efficiency with measurement uncertainty')\nax.legend()\nax.grid(True)\n\nplt.show()\n\nThe error bars are largest relative to the measured value at low rotational\nspeed, because the efficiency itself is small there. The best operating point\nat 2000 1/min, on the other hand, is determined very precisely. The\nzorder=5 ensures that the star lies above the error bars.\n\nClosing question\n\nAnswer in your own words, without further code:\n\nThe motor delivers its largest mechanical power at 1500 1/min, but works\nmost efficiently at 2000 1/min. For which rotational speed would you design\nthe motor for continuous operation? Justify.\n\nAt which measured points is the relative measurement uncertainty of the\nefficiency largest? What follows from this for planning further\nmeasurements?\n\nSolution closing question\n\nFor continuous operation, one usually chooses a rotational speed close to\nthe best efficiency, that is, about 2000 1/min. There, the least energy is\nlost as heat, the motor runs cooler and the operating costs are lower. The\nmaximum power at 1500 1/min is more of interest for short-term load peaks.\nIf the application permanently needs the highest power, the lower\nefficiency must be deliberately accepted.\n\nThe relative uncertainty is the quotient of efficiency_std and\nefficiency. It is largest at the low rotational speeds, because the\nefficiency itself is small there, while the absolute scatter stays\nsimilar. At 250 1/min it is over ten percent. For further measurements it\nis worth taking more repeats in the lower rotational-speed range or using\na more accurate measurement method. The well-measured range around\n2000 1/min, on the other hand, needs less effort.\n\nBonus exercise: Second test-rig run with a warm motor (✩✩✩)\n\nAfter an hour of continuous running, the motor is warm. A second test-rig run\nat the same rotational speeds and the same torque delivers a higher power\ninput, because the winding resistances rise with temperature:power_input_warm = np.array([1420, 1780, 1880, 1680, 1370, 1100,\n                             910, 830, 770, 710, 660, 560])   # W\n\nCompute the efficiency efficiency_warm for the warm run. The torque, and\nthus p_mech, stay unchanged.\n\nPlot both efficiency curves, cold and warm, in a common diagram, with a\nlegend.\n\nCompare: how much does the peak efficiency drop? Does the rotational speed\nof the best operating point shift?\n\n# code cell\n\n\n\nSolution bonus exercisepower_input_warm = np.array([1420, 1780, 1880, 1680, 1370, 1100,\n                             910, 830, 770, 710, 660, 560])\n\nefficiency_warm = p_mech / power_input_warm\n\nfig, ax = plt.subplots(figsize=(7, 4))\nax.plot(rpm, efficiency,      marker='o', label='Cold motor')\nax.plot(rpm, efficiency_warm, marker='o', label='Warm motor')\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Efficiency with a cold and a warm motor')\nax.set_ylim(0, 1)\nax.legend()\nax.grid(True)\nplt.show()\n\nprint(f\"Peak efficiency cold: {np.max(efficiency):.2f} \"\n      f\"at {rpm[np.argmax(efficiency)]} 1/min\")\nprint(f\"Peak efficiency warm: {np.max(efficiency_warm):.2f} \"\n      f\"at {rpm[np.argmax(efficiency_warm)]} 1/min\")\n\nThe peak efficiency drops from about 0.84 to about 0.73, that is, by more than\nten percentage points. The rotational speed of the best operating point\nstays at 2000 1/min. The motor temperature therefore noticeably worsens the\nefficiency without shifting the location of the optimum. For a reliable\nreport, the operating temperature at which the measurement was taken should\nalways be stated.","type":"content","url":"/chapter02-sec04","position":1},{"hierarchy":{"lvl1":"2.5 Exercises"},"type":"lvl1","url":"/chapter02-sec05","position":0},{"hierarchy":{"lvl1":"2.5 Exercises"},"content":"These exercises are intended for self-study at home and review the material\nof chapters 2.1 to 2.4. Expect around 90 minutes of working time.\n\nThe difficulty level is given in the title of each exercise:\n\n✩ Comprehension: predict and explain code and outputs (approx. 5 min)\n\n✩✩ Application: write your own code and interpret results (approx. 10 min)\n\n✩✩✩ Mini-project: combine several concepts of the part (approx. 30 min)\n\nExercise 2.1 (✩)\n\nThe following code is given:import numpy as np\n\nt = np.linspace(0, 10, 5)\nplaceholder = np.zeros(3)\ncounter = np.array([2, 4, 6])\nmeasurement = np.array([2, 4, 6.0])\n\nNote down your guess before you run the code.\n\nWhich values does t contain and what does t.shape return?\n\nWhat does placeholder.dtype return?\n\nWhat does counter.dtype return and what does measurement.dtype? Why do\nthe two differ, even though only one number is written differently?\n\nRun the code and check your predictions.\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\nt = np.linspace(0, 10, 5)\nplaceholder = np.zeros(3)\ncounter = np.array([2, 4, 6])\nmeasurement = np.array([2, 4, 6.0])\n\nprint(t)\nprint(t.shape)\nprint(placeholder.dtype)\nprint(counter.dtype)\nprint(measurement.dtype)\n\nOutput:[ 0.   2.5  5.   7.5 10. ]\n(5,)\nfloat64\nint64\nfloat64\n\nt contains five evenly distributed values from 0 to 10, t.shape is\n(5,), that is, one dimension with five elements. np.zeros() and\nnp.linspace() always generate floating-point numbers. counter contains\nonly whole numbers, so NumPy chooses int64. In measurement, the single\ndecimal number 6.0 forces all values to be stored as float64, because an\narray has exactly one data type.\n\nExercise 2.2 (✩)\n\nTwo measurement series are given:import numpy as np\n\nmeasurements_1 = np.array([2.0, 4.0, 6.0, 8.0])\nmeasurements_2 = np.array([1.0, 2.0, 3.0, 4.0])\nangle = np.array([0.0, np.pi / 2, np.pi])\n\nNote down your guess before you run the code.\n\nWhat do measurements_1 + measurements_2, measurements_1 * measurements_2\nand measurements_1 / measurements_2 return?\n\nWhat does measurements_2 ** 2 return?\n\nWhat does np.sin(angle) return? Why is the value not exactly 0 at the\nposition of \\pi?\n\nRun the code and check your predictions.\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\nmeasurements_1 = np.array([2.0, 4.0, 6.0, 8.0])\nmeasurements_2 = np.array([1.0, 2.0, 3.0, 4.0])\nangle = np.array([0.0, np.pi / 2, np.pi])\n\nprint(measurements_1 + measurements_2)\nprint(measurements_1 * measurements_2)\nprint(measurements_1 / measurements_2)\nprint(measurements_2 ** 2)\nprint(np.sin(angle))\n\nOutput:[ 3.  6.  9. 12.]\n[ 2.  8. 18. 32.]\n[2. 2. 2. 2.]\n[ 1.  4.  9. 16.]\n[0.0000000e+00 1.0000000e+00 1.2246468e-16]\n\nAll basic arithmetic operations act element-wise; each element is combined\nwith the element at the same position. np.sin(np.pi) does not yield exactly\n0, but a tiny value on the order of 1e-16. This is a rounding error of\nfloating-point arithmetic, because \\pi cannot be stored exactly in the\ncomputer.\n\nExercise 2.3 (✩)\n\nOn a hydraulic test rig, the system pressure is measured eight times, in bar:import numpy as np\n\npressure = np.array([4.9, 5.1, 5.0, 4.8, 5.2, 5.0, 4.95, 5.05])\n\nAnswer first without code:\n\nIs np.mean(pressure) closer to 5.0 or to 5.5?\n\nIs np.std(pressure) closer to 0.1 or to 1.0? Justify with a look at the\nmeasured values.\n\nWhat does np.max(pressure) - np.min(pressure) compute in terms of\ncontent?\n\nRun the code and check your predictions.\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\npressure = np.array([4.9, 5.1, 5.0, 4.8, 5.2, 5.0, 4.95, 5.05])\n\nprint(np.mean(pressure))\nprint(np.std(pressure))\nprint(np.max(pressure) - np.min(pressure))\n\nOutput:5.0\n0.11456439237389597\n0.40000000000000036\n\nThe mean is exactly 5.0 bar. The standard deviation is about 0.11 bar and\nthus close to 0.1, because all measured values lie close to the mean, in the\nband between 4.8 and 5.2 bar. np.max(pressure) - np.min(pressure) is the\nrange, that is, the distance between the largest and smallest measured value,\nhere 0.4 bar.\n\nExercise 2.4 (✩)\n\nThe following code is given:import numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\nt = np.linspace(0, 1, 500)\n\nfig, ax = plt.subplots(nrows=2, ncols=1, figsize=(8, 6))\nax[0].plot(t, np.sin(2 * np.pi * 3 * t), linestyle='dashed', label='3 Hz')\nax[1].plot(t, np.exp(-2 * t))\nplt.tight_layout()\nplt.show()\n\nNote down your guess before you run the code.\n\nWhich line style does the upper curve have and how many full oscillations\ndoes it show in the plotted range?\n\nThe upper plot call has label='3 Hz', but there is no call to\nax[0].legend(). What can be seen in the diagram as a result?\n\nHow many subplots does plt.subplots(nrows=2, ncols=1) create and how do\nyou address the lower one?\n\nRun the code and check your predictions.\n\n# code cell\n\n\n\nSolutionimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\nt = np.linspace(0, 1, 500)\n\nfig, ax = plt.subplots(nrows=2, ncols=1, figsize=(8, 6))\nax[0].plot(t, np.sin(2 * np.pi * 3 * t), linestyle='dashed', label='3 Hz')\nax[1].plot(t, np.exp(-2 * t))\nplt.tight_layout()\nplt.show()\n\nThe upper curve is dashed (linestyle='dashed') and shows three full\noscillations, since the frequency is 3 Hz and the range is one second\nlong.\n\nNothing. label='3 Hz' only stores the label internally. Only\nax[0].legend() would turn it into a visible legend.\n\nplt.subplots(nrows=2, ncols=1) creates two subplots one below the other.\nThe lower one is addressed with ax[1], the upper one with ax[0].\n\nExercise 2.5 (✩✩)\n\nOn a test rig, the holding force of a bolted joint is measured eight times,\nin newtons:force_n = np.array([812, 798, 825, 803, 819, 807, 830, 815])\n\nCompute the mean, standard deviation, minimum and maximum and print the\nresults formatted.\n\nThe joint is considered acceptable if all measured values lie between\n780 N and 840 N. Check this with an if check over np.min(force_n) and\nnp.max(force_n) and print a suitable message.\n\nConvert the measurement series to kilonewtons (1 kN = 1000 N) and print\nthe array force_kn.\n\nStructure your code with IPO comments (input, processing, output).\n\n# code cell\n\n\n\nSolutionimport numpy as np\n\n# Input\nforce_n = np.array([812, 798, 825, 803, 819, 807, 830, 815])\nlower_limit = 780\nupper_limit = 840\n\n# Processing\nmean = np.mean(force_n)\nspread = np.std(force_n)\nforce_min = np.min(force_n)\nforce_max = np.max(force_n)\nforce_kn = force_n / 1000\n\n# Output\nprint(f\"Mean:               {mean:.1f} N\")\nprint(f\"Standard deviation: {spread:.1f} N\")\nprint(f\"Minimum:            {force_min} N\")\nprint(f\"Maximum:            {force_max} N\")\n\nif force_min >= lower_limit and force_max <= upper_limit:\n    print(\"All measured values within the tolerance range.\")\nelse:\n    print(\"Tolerance range exceeded!\")\n\nprint(f\"Measurement series in kN: {force_kn}\")\n\nOutput:Mean:               813.6 N\nStandard deviation: 10.2 N\nMinimum:            798 N\nMaximum:            830 N\nAll measured values within the tolerance range.\nMeasurement series in kN: [0.812 0.798 0.825 0.803 0.819 0.807 0.83  0.815]\n\nThe smallest value is 798 N, the largest 830 N. Both lie within the limits of\n780 N and 840 N, so the joint is acceptable. The division by 1000 acts as a\nvector operation on every element.\n\nExercise 2.6 (✩✩)\n\nA hot component cools down in still air. The temperature follows the decay\nlawT(t) = T_\\text{ambient} + (T_\\text{start} - T_\\text{ambient}) \\cdot e^{-t / \\tau}\n\nWrite a function\ncomponent_temperature(time, t_ambient, t_start, tau) that returns the\ntemperature as a NumPy array. Give the function a docstring.\n\nCall the function for a time axis from 0 to 600 s with 100 points, with\nt_ambient = 20, t_start = 200 and tau = 150.\n\nPlot the temperature curve as a line plot, with axis labels, title and\ngrid.\n\nStructure your code with IPO comments (input, processing, output).\n\n# code cell\n\n\n\nSolutionimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\n# Input\ndef component_temperature(time, t_ambient, t_start, tau):\n    \"\"\"Computes the cooling curve of a component in still air.\n\n    time:      time axis as a NumPy array in s\n    t_ambient: ambient temperature in degrees Celsius\n    t_start:   initial temperature of the component in degrees Celsius\n    tau:       time constant of the cooling in s\n    Returns:   temperature as a NumPy array in degrees Celsius\n    \"\"\"\n    return t_ambient + (t_start - t_ambient) * np.exp(-time / tau)\n\ntime = np.linspace(0, 600, 100)\n\n# Processing\ntemperature = component_temperature(time, 20, 200, 150)\n\n# Output\nfig, ax = plt.subplots(figsize=(7, 4))\nax.plot(time, temperature)\nax.set_xlabel('Time in s')\nax.set_ylabel('Temperature in degrees Celsius')\nax.set_title('Cooling of a component in still air')\nax.grid(True)\nplt.show()\n\nAfter 600 s the temperature has fallen to about 23 degrees Celsius and is\nthus almost at ambient level. The curve falls steeply at the start and\nbecomes flatter and flatter as the temperature difference decreases.\n\nExercise 2.7 (✩✩)\n\nThe same component is cooled once in still air (tau = 200) and once with a\nfan (tau = 80). In both cases the ambient temperature is 20 degrees Celsius\nand the initial temperature is 200 degrees Celsius.\n\nCompute both temperature curves for a time axis from 0 to 600 s with 100\npoints. Use the formula from exercise 2.6.\n\nPlot both curves in a common diagram, with a legend, axis labels, title\nand grid.\n\nStructure your code with IPO comments (input, processing, output).\n\n# code cell\n\n\n\nSolutionimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\n# Input\ntime = np.linspace(0, 600, 100)\nt_ambient = 20\nt_start = 200\n\n# Processing\ntemperature_still_air = t_ambient + (t_start - t_ambient) * np.exp(-time / 200)\ntemperature_fan = t_ambient + (t_start - t_ambient) * np.exp(-time / 80)\n\n# Output\nfig, ax = plt.subplots(figsize=(7, 4))\nax.plot(time, temperature_still_air, label='Still air')\nax.plot(time, temperature_fan, label='With fan')\nax.set_xlabel('Time in s')\nax.set_ylabel('Temperature in degrees Celsius')\nax.set_title('Cooling with and without a fan')\nax.legend()\nax.grid(True)\nplt.show()\n\nWith a fan the time constant is smaller, the component cools down\nconsiderably faster. After 240 s it is almost at ambient temperature with a\nfan, while in still air it is still over 70 degrees Celsius hot.\n\nExercise 2.8 (✩✩)\n\nThe efficiency of a gearbox is measured at six rotational speeds, each value\nas the mean of several repeats with a standard deviation:rpm = np.array([500, 1000, 1500, 2000, 2500, 3000])\nefficiency = np.array([0.89, 0.93, 0.95, 0.96, 0.94, 0.91])\nefficiency_std = np.array([0.010, 0.008, 0.006, 0.006, 0.009, 0.012])\n\nPlot the efficiency over the rotational speed with ax.errorbar() and\nerror bars in the y-direction (fmt='o', capsize=4).\n\nDetermine the point with the best efficiency with np.argmax() and draw\nit with a second ax.scatter() call as a large star (marker='*',\ns=200).\n\nLabel the axes and title and show a legend.\n\nStructure your code with IPO comments (input, processing, output).\n\n# code cell\n\n\n\nSolutionimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\n# Input\nrpm = np.array([500, 1000, 1500, 2000, 2500, 3000])\nefficiency = np.array([0.89, 0.93, 0.95, 0.96, 0.94, 0.91])\nefficiency_std = np.array([0.010, 0.008, 0.006, 0.006, 0.009, 0.012])\n\n# Processing\ni_best = np.argmax(efficiency)\n\n# Output\nfig, ax = plt.subplots(figsize=(7, 4))\nax.errorbar(rpm, efficiency, yerr=efficiency_std,\n            fmt='o', capsize=4, label='Measurement')\nax.scatter(rpm[i_best], efficiency[i_best],\n           marker='*', s=200, color='red', zorder=5, label='Best efficiency')\nax.set_xlabel('Rotational speed in 1/min')\nax.set_ylabel('Efficiency')\nax.set_title('Gearbox efficiency with measurement uncertainty')\nax.legend()\nax.grid(True)\nplt.show()\n\nThe best efficiency is at 2000 1/min with 0.96. Between 1500 and 2500 1/min\nthe curve is almost flat, so the gearbox works with good efficiency over a\nwide rotational-speed range.\n\nExercise 2.9 (✩✩✩) Mini-project: Characteristic curve of a centrifugal pump\n\nOn a pump test rig, the volume flow rate is increased in eight steps. At each\nstep, the delivery head and the electrical power input are measured:flow_rate = np.array([2, 4, 6, 8, 10, 12, 14, 16])                # l/s\nhead = np.array([48, 47, 45, 42, 38, 33, 26, 18])                 # m\npower_input = np.array([2700, 3550, 4080, 4520,\n                        4970, 5400, 5760, 6280])                  # W\n\nPart 1: Create the three measurement series as arrays and check with\n.shape that they are all the same length. Plot the delivery head over the\nvolume flow rate as a scatter plot.\n\nPart 2: Compute the hydraulic power\np_hydraulic = 1000 * 9.81 * flow_rate_m3s * head, where\nflow_rate_m3s = flow_rate / 1000 is the volume flow rate in m³/s. Compute\nthe efficiency efficiency = p_hydraulic / power_input.\n\nPart 3: Create a Figure with three subplots one below the other: delivery\nhead, hydraulic power and efficiency, each over the volume flow rate. Give\nthe efficiency subplot the full scale with set_ylim(0, 1). Set the x-axis\nlabel and the grid via a for loop.\n\nPart 4: Determine the volume flow rate with the best efficiency with\nnp.argmax(). Also compute the mean, standard deviation and maximum of the\nhydraulic power. Print the results as a short text report with f-strings.\n\nClosing question: The best efficiency and the largest hydraulic power\noccur at different volume flow rates. For which volume flow rate should the\npump be designed for continuous operation, and what does the falling delivery\nhead mean for operation if suddenly more flow is demanded?\n\nStructure your code with IPO comments (input, processing, output).\n\n# code cell\n\n\n\nSolutionimport numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.style as style\nstyle.use('seaborn-v0_8')\n\n# Input\nflow_rate = np.array([2, 4, 6, 8, 10, 12, 14, 16])\nhead = np.array([48, 47, 45, 42, 38, 33, 26, 18])\npower_input = np.array([2700, 3550, 4080, 4520,\n                        4970, 5400, 5760, 6280])\n\nprint(flow_rate.shape, head.shape, power_input.shape)\n\n# Processing\nflow_rate_m3s = flow_rate / 1000\np_hydraulic = 1000 * 9.81 * flow_rate_m3s * head\nefficiency = p_hydraulic / power_input\n\nflow_rate_best_efficiency = flow_rate[np.argmax(efficiency)]\nmean_power = np.mean(p_hydraulic)\npower_spread = np.std(p_hydraulic)\nmax_power = np.max(p_hydraulic)\n\n# Output: scatter plot\nfig, ax = plt.subplots(figsize=(7, 4))\nax.scatter(flow_rate, head)\nax.set_xlabel('Volume flow rate in l/s')\nax.set_ylabel('Delivery head in m')\nax.set_title('Measured delivery head over the volume flow rate')\nax.grid(True)\nplt.show()\n\n# Output: characteristic map\nfig, ax = plt.subplots(nrows=3, ncols=1, figsize=(7, 8))\n\nax[0].plot(flow_rate, head)\nax[0].set_ylabel('Delivery head in m')\n\nax[1].plot(flow_rate, p_hydraulic)\nax[1].set_ylabel('Hydraulic power in W')\n\nax[2].plot(flow_rate, efficiency)\nax[2].set_ylabel('Efficiency')\nax[2].set_ylim(0, 1)\n\nfor single_axis in ax:\n    single_axis.set_xlabel('Volume flow rate in l/s')\n    single_axis.grid(True)\n\nax[0].set_title('Characteristic map of the centrifugal pump')\nplt.tight_layout()\nplt.show()\n\n# Output: text report\nprint(f\"Best efficiency at {flow_rate_best_efficiency} l/s\")\nprint(f\"Hydraulic power: mean {mean_power:.0f} W, \"\n      f\"spread {power_spread:.0f} W, maximum {max_power:.0f} W\")\n\nOutput:(8,) (8,) (8,)\nBest efficiency at 10 l/s\nHydraulic power: mean 2842 W, spread 952 W, maximum 3885 W\n\nThe best efficiency of about 0.75 is at 10 l/s, the largest hydraulic power\nof about 3885 W only at 12 l/s.\n\nClosing question: For continuous operation, the pump is designed for the\nvolume flow rate with the best efficiency, that is, about 10 l/s. There, the\nleast drive power is lost as losses. The falling delivery head means that the\npump can build up less and less pressure at higher flow. If the system\nsuddenly demands more volume flow, the delivery head drops, and beyond a\ncertain point the pressure is no longer sufficient to supply the system\nagainst its resistance.","type":"content","url":"/chapter02-sec05","position":1},{"hierarchy":{"lvl1":"ANM"},"type":"lvl1","url":"/","position":0},{"hierarchy":{"lvl1":"ANM"},"content":"TH Mannheim markdownlint-disable MD001 ","type":"content","url":"/","position":1},{"hierarchy":{"lvl1":"ANM","lvl2":"ANM Lecture Notes"},"type":"lvl2","url":"/#anm-lecture-notes","position":2},{"hierarchy":{"lvl1":"ANM","lvl2":"ANM Lecture Notes"},"content":" markdownlint-enable MD001 \n\nThese lecture notes support the course “Applied Numerical Mathematics”. Many\nexamples have been adapted from the lecture notes “Applied Numerical Methods in\nMechanical Engineering” by Alexander Baral.","type":"content","url":"/#anm-lecture-notes","position":3}]}